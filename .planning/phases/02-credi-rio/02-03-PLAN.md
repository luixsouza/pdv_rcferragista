---
phase: 02-credi-rio
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/pages/CreditNotes.tsx
autonomous: true
requirements: [CRED-03]
must_haves:
  truths:
    - "When registering payment of an overdue installment, the operator sees the calculated interest amount in the dialog"
    - "An explicit 'cobrar juros' action pre-fills the payment amount with roundCurrency(remaining + interest)"
    - "Interest is never auto-applied — the operator must click the action to include it"
    - "When interest is charged, the CreditPayment record stores the interest component auditably"
  artifacts:
    - path: "src/pages/CreditNotes.tsx"
      provides: "Explicit interest-charging action in the payment dialog + interest recorded on the CreditPayment"
      contains: "interestAmount"
  key_links:
    - from: "src/pages/CreditNotes.tsx handlePayment"
      to: "CreditPayment.interestAmount"
      via: "payment record construction includes interest component"
      pattern: "interestAmount"
---

<objective>
Turn the display-only interest into an explicit, auditable charge in the payment dialog of the Crediário screen.

Purpose: CRED-03 requires that, for an overdue installment, the operator sees the calculated interest and can explicitly charge it (a button that pre-fills the payment amount with saldo + juros using roundCurrency), and that the interest component is recorded auditably in CreditPayment. Today (CONCERNS.md "Interest Calculation Is Display-Only", lines 259-266) the juros is informational and the operator must manually type it — it is easily forgotten and leaves no audit trail. Interest must NEVER be auto-applied (CONTEXT CRED-03).

Output: Updated `src/pages/CreditNotes.tsx` payment dialog and handlePayment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-credi-rio/02-CONTEXT.md
@.planning/phases/02-credi-rio/02-01-SUMMARY.md
@.planning/phases/02-credi-rio/02-02-SUMMARY.md

<interfaces>
<!-- CreditPayment now carries the optional interest field (added in Plan 01) -->
```typescript
export interface CreditPayment {
  id: string;
  saleId: string;
  installmentId?: string;
  clientId: string;
  clientName: string;
  amount: number;                 // total registered (may include interest)
  paymentMethod: 'cash' | 'credit' | 'debit' | 'pix';
  type?: 'payment' | 'discount';
  interestAmount?: number;        // interest component included within amount (added Plan 01)
  createdAt: string;
}
```

<!-- Existing CreditNotes.tsx structures the executor must extend -->
- calculateInterest(inst): number — lines 79-87. Returns 0 unless status==='overdue' and interestRate>0; = remaining * (interestRate/100) * (daysOverdue/30). Uses persisted status today; Plan 02 made the screen use effective status, so guard interest on the EFFECTIVE overdue status.
- openPaymentDialog(inst) — lines 168-174 — sets selectedInstallment, paymentAmount=remaining, paymentMethod='cash'.
- handlePayment() — lines 176-258 — validates paymentAmount, builds CreditPayment, updates installment.amountPaid/status, updates sale.crediarioPaid/status, toasts.
- Payment dialog JSX — lines 907-1036. Interest is already DISPLAYED at lines 952-960 (when status overdue). The "Valor do pagamento" input is at lines 995-1008.
- roundCurrency, formatCurrency: from `@/lib/formatters`.
- getEffectiveStatus: from `@/lib/installmentStatus` (already imported by Plan 02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit "cobrar juros" action to the payment dialog</name>
  <files>src/pages/CreditNotes.tsx</files>
  <read_first>
    - src/pages/CreditNotes.tsx lines 76-87 (interestRate + calculateInterest)
    - src/pages/CreditNotes.tsx lines 167-174 (openPaymentDialog)
    - src/pages/CreditNotes.tsx lines 907-1036 (payment dialog JSX: interest display at 952-960, payment input at 995-1008, action buttons at 1025-1032)
    - src/lib/installmentStatus.ts (getEffectiveStatus — interest must guard on effective overdue)
  </read_first>
  <action>
    In the payment dialog, when the selected installment is overdue by EFFECTIVE status (use getEffectiveStatus(selectedInstallment), not the raw persisted status — consistent with Plan 02) and calculateInterest returns > 0, render an explicit "Cobrar juros" action button near the existing interest display (lines 952-960) / above the payment input. Clicking it sets paymentAmount to `roundCurrency(remaining + interest)` where remaining = amount - amountPaid - (discountApplied||0) and interest = calculateInterest(selectedInstallment). Track whether interest was charged in a piece of component state (e.g. `chargedInterest` number, default 0), set when the button is clicked and reset to 0 in openPaymentDialog and when the dialog closes. Interest is NEVER pre-applied automatically: openPaymentDialog must continue to set paymentAmount = remaining (without interest) — the operator opts in via the button only (CONTEXT CRED-03: "Não tornar a cobrança de juros automática/silenciosa"). Update calculateInterest's status guard to use getEffectiveStatus so a freshly-lapsed installment also shows interest. Display the calculated interest value with formatCurrency (it is already shown at 952-960 — keep/adjust that block). Add a small hint when interest has been charged (e.g. the input reflects saldo + juros). Reuse existing Button/Label/colors — no new components.
  </action>
  <verify>
    <automated>cd /c/dev/pdv_rcferragista && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Cobrar juros" src/pages/CreditNotes.tsx` returns >= 1 (the explicit action exists).
    - `grep -c "getEffectiveStatus" src/pages/CreditNotes.tsx` increased vs. Plan 02 baseline OR calculateInterest now guards on effective status.
    - openPaymentDialog still sets paymentAmount to remaining WITHOUT interest (interest not auto-applied) — verify the function body does not add calculateInterest to the initial paymentAmount.
    - `chargedInterest` (or equivalent) state is reset in openPaymentDialog.
    - `npm run build` exits 0.
    - Observable: opening the dialog for an overdue installment shows the juros value and a "Cobrar juros" button; the payment amount starts at saldo (no juros) until the button is clicked, after which it equals roundCurrency(saldo + juros).
  </acceptance_criteria>
  <done>Payment dialog shows calculated interest and an explicit charge button that pre-fills saldo+juros; interest is opt-in only; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Record the interest component auditably in handlePayment</name>
  <files>src/pages/CreditNotes.tsx</files>
  <read_first>
    - src/pages/CreditNotes.tsx lines 176-258 (handlePayment: validation, CreditPayment construction at 198-207, installment update, sale update, toasts)
    - src/types/index.ts lines 71-81 (CreditPayment with interestAmount? from Plan 01)
  </read_first>
  <action>
    In handlePayment, when interest was charged (chargedInterest > 0 from Task 1), populate `interestAmount: chargedInterest` on the constructed CreditPayment record (lines 198-207) so the charge is auditable (CRED-03). The over-payment guard at lines 188-191 currently rejects paymentAmount > remaining + 0.01; this MUST allow the interest portion — adjust the guard so the cap is `remaining + chargedInterest + 0.01` when interest was charged (do NOT raise the cap when no interest was charged). When applying the payment to the installment, the interest is paid ON TOP of the installment balance: do NOT let the interest inflate amountPaid beyond the effective amount in a way that mis-marks fully-paid detection. Specifically, compute the principal portion = min(paymentAmount, remaining) for amountPaid/isFullyPaid logic (lines 194-195), and treat the excess (= chargedInterest) purely as recorded interest income on the CreditPayment + reflected in sale.crediarioPaid total. Reset chargedInterest to 0 after a successful payment (alongside setSelectedInstallment(null) at line 247). Keep all existing toasts; optionally append the interest amount to the success toast when interestAmount > 0. Do NOT change the discount flow. Do NOT modify installment.amount (interest is not capitalized into the principal — keeps the simple model per CONTEXT deferred "Juros compostos fora de escopo").
  </action>
  <verify>
    <automated>cd /c/dev/pdv_rcferragista && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "interestAmount" src/pages/CreditNotes.tsx` returns >= 1 (interest recorded on the payment).
    - The over-payment guard accounts for chargedInterest (allows saldo+juros, still rejects beyond that).
    - isFullyPaid / amountPaid logic uses the principal portion (min(paymentAmount, remaining)), so charging interest still correctly marks the installment paid when the principal is covered.
    - chargedInterest is reset after a successful payment.
    - installment.amount is NOT mutated by interest (interest not capitalized).
    - `npm run build` exits 0.
    - Observable: charging juros on an overdue installment quits the parcela (status paid when principal covered), records a CreditPayment whose interestAmount equals the charged juros, and sale.crediarioPaid reflects principal + interest.
  </acceptance_criteria>
  <done>handlePayment records interestAmount on the CreditPayment, allows the saldo+juros total, marks the installment paid on principal coverage, and does not capitalize interest into the principal; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → installment payment mutation | Operator-entered payment amount + opt-in interest crosses into persisted CreditPayment/Installment/Sale records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | over-payment guard with interest | mitigate | Guard caps payment at remaining + chargedInterest + 0.01; interest cap only applies when explicitly charged, preventing silent over-collection. |
| T-02-06 | Repudiation | interest charge audit | mitigate | interestAmount persisted on CreditPayment creates an audit trail (fixes CONCERNS "no audit trail of interest charged"). |
| T-02-07 | Elevation/Integrity | auto-applied interest | mitigate | Interest is opt-in via explicit button only; openPaymentDialog never pre-applies it (CONTEXT CRED-03 hard rule). |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No new dependency surface. |
</threat_model>

<verification>
- `npm run build` exits 0.
- Payment dialog shows calculated interest and an explicit "Cobrar juros" button for effectively-overdue installments.
- openPaymentDialog never pre-applies interest; button click sets roundCurrency(saldo + juros).
- handlePayment records interestAmount on the CreditPayment and allows the saldo+juros total.
- Installment is marked paid on principal coverage; installment.amount not capitalized with interest.
</verification>

<success_criteria>
- CRED-03: operator sees calculated interest and can charge it explicitly before confirming payment; interest is auditable on CreditPayment; interest never auto-applied.
- Existing discount and non-overdue payment flows unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-credi-rio/02-03-SUMMARY.md` when done.
</output>
