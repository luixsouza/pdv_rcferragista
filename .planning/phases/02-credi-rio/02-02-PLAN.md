---
phase: 02-credi-rio
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/pages/CreditNotes.tsx
autonomous: true
requirements: [CRED-01, CRED-02]
must_haves:
  truths:
    - "The Crediário screen shows, per client and per sale, total devido, total pago, saldo em aberto, and a status count of each installment (open/overdue/paid/cancelled)"
    - "Cancelled installments are excluded from devido and saldo"
    - "An installment that became overdue while the app stayed open displays as Vencida without reloading the page"
  artifacts:
    - path: "src/pages/CreditNotes.tsx"
      provides: "Per-client/per-sale summary using on-the-fly overdue status from the shared helper"
      contains: "getEffectiveStatus"
  key_links:
    - from: "src/pages/CreditNotes.tsx"
      to: "src/lib/installmentStatus.ts"
      via: "import of getEffectiveStatus/isInstallmentOverdue"
      pattern: "installmentStatus"
---

<objective>
Enhance the existing Crediário screen so balances and statuses reflect reality: per-client and per-sale totals (devido / pago / saldo) with status counts, and overdue computed on-the-fly at render via the shared helper.

Purpose: CRED-01 requires a clear per-client and per-sale view of total devido (sum of non-cancelled installments), total pago (amountPaid + descontos), saldo em aberto, and the situation of each installment. CRED-02 requires that display-time overdue detection be reliable even when the app stays open past midnight — so this plan switches the screen's rendering and aggregation off the persisted `status` and onto `getEffectiveStatus` from Plan 01.

Output: Updated `src/pages/CreditNotes.tsx` reusing the existing cards/badges/formatCurrency (no new design system, per CONTEXT decision).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-credi-rio/02-CONTEXT.md
@.planning/phases/02-credi-rio/02-01-SUMMARY.md

<interfaces>
<!-- From src/lib/installmentStatus.ts (created in Plan 01) -->
```typescript
export const isInstallmentOverdue: (inst: Installment, now?: Date) => boolean;
export const getEffectiveStatus: (inst: Installment, now?: Date) => Installment['status'];
// getEffectiveStatus returns 'overdue' for an 'open' installment past its dueDate,
// 'overdue' for an already-'overdue' record, and the persisted status otherwise.
// 'cancelled' and 'paid' are returned unchanged.
```

<!-- Existing CreditNotes.tsx structures the executor must preserve/extend -->
- roundCurrency, formatCurrency, paymentLabels: from `@/lib/formatters` (roundCurrency added Phase 1).
- The "Resumo" tab already renders a per-client card (lines 392-528) with creditLimit/used/available and a 3-cell status count grid (open/overdue/paid) at lines 464-520.
- The "Parcelas" tab (lines 532-645) lists installments and color-codes by status; getStatusBadge (lines 342-351) renders the badge.
- filteredInstallments (lines 105-120) sorts overdue→open→paid and hides number===0 and cancelled.
- Installment fields: amount, amountPaid, discountApplied?, status, number (0 = entry), saleId, totalInstallments, dueDate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute display status on-the-fly via the shared helper</name>
  <files>src/pages/CreditNotes.tsx</files>
  <read_first>
    - src/pages/CreditNotes.tsx lines 1-35 (imports)
    - src/pages/CreditNotes.tsx lines 60-120 (mount useEffect, calculateInterest, resumo calcs, filteredInstallments)
    - src/pages/CreditNotes.tsx lines 139-165 (totalPendingInstallments, overdueInstallments, delinquentClients)
    - src/pages/CreditNotes.tsx lines 342-351 and 562-642 (getStatusBadge + Parcelas card render, which branch on inst.status)
    - src/lib/installmentStatus.ts (helper from Plan 01)
  </read_first>
  <action>
    Import `getEffectiveStatus` and `isInstallmentOverdue` from `@/lib/installmentStatus` (add to the import block near line 34-35). Introduce a single derived helper usage so every place that currently reads `inst.status` for DISPLAY or AGGREGATION uses the effective status instead: define a small local `const effStatus = getEffectiveStatus(inst)` at each render/aggregation site (or map installments once to an augmented shape `{ ...inst, status: getEffectiveStatus(inst) }` in a useMemo near the top and feed that derived array into filteredInstallments, overdueInstallments, delinquentClients, totalPendingInstallments, and the resumo status counts). Prefer the single useMemo-derived array approach to avoid drift (map each installment to `{ ...inst, status: getEffectiveStatus(inst) }` once and derive ALL of the following from that mapped array so none is missed). Update: (a) filteredInstallments status filter AND the sort comparator key at line ~116 (`{overdue:0,open:1,paid:2}[a.status]`/`[b.status]`) to use effective status — if derived from the mapped array, `a.status`/`b.status` are already effective and line 116 needs no extra change, but it MUST end up sorting on effective status; (b) overdueInstallments (line ~144) to use effective status; (c) the resumo status-count grid counts (lines ~466-468) to use effective status; (d) **`resumoOverdue` and `resumoOverdueAmount` (lines ~99-101)** — these currently filter `installments.filter(i => i.status === 'overdue')` (raw) and drive the Resumo tab "Valor vencido"; they MUST switch to effective status too; (e) getStatusBadge calls and the Parcelas card color branches (lines 569-635) to receive the effective status. Keep the mount useEffect (lines 61-74) so persisted status still eventually catches up for OTHER pages — do NOT remove it (POS uses the shared helper now, but the persisted write remains harmless and keeps the historical behavior). Do NOT change calculateInterest in this plan (Plan 03 owns interest). Keep using existing cards/badges/formatCurrency — no new components.
  </action>
  <verify>
    <automated>cd /c/dev/pdv_rcferragista && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getEffectiveStatus" src/pages/CreditNotes.tsx` returns >= 2 (import + at least one usage).
    - filteredInstallments (filter AND sort), overdueInstallments, `resumoOverdue`/`resumoOverdueAmount`, and the resumo status counts all derive from effective status (not raw `inst.status` for display). No remaining `i.status === 'overdue'` / `inst.status === 'overdue'` raw comparisons drive display or aggregation in this file.
    - The mount useEffect at lines ~61-74 is still present (not deleted).
    - `npm run build` exits 0.
    - Observable: an `open` installment with a past dueDate appears as Vencida in the Parcelas list and is counted under "Vencidas" in the resumo, without navigating away and back.
  </acceptance_criteria>
  <done>All display/aggregation paths in CreditNotes use effective (on-the-fly) overdue status; build passes; an installment that lapsed while the app was open shows as overdue immediately.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-client and per-sale balance summary</name>
  <files>src/pages/CreditNotes.tsx</files>
  <read_first>
    - src/pages/CreditNotes.tsx lines 89-102 (existing resumo per-client aggregates: resumoCreditUsed, resumoOverdueAmount)
    - src/pages/CreditNotes.tsx lines 463-520 (existing "Parcelas do Cliente" card with the 3-cell status grid — where the per-sale breakdown should be added)
    - src/pages/CreditNotes.tsx lines 562-642 (Parcelas card layout/style to reuse: Card/CardContent, formatCurrency, status colors)
    - src/lib/formatters.ts (roundCurrency, formatCurrency)
  </read_first>
  <action>
    In the Resumo tab, extend the selected client's view with explicit totals and a per-sale breakdown (CRED-01). Compute, from the effective-status installments for the selected client (number !== 0 grouped where appropriate; entry installment number===0 is always paid and counts toward pago): totalDevido = roundCurrency(sum over non-cancelled installments of amount - (discountApplied||0)); totalPago = roundCurrency(sum over non-cancelled installments of amountPaid + (discountApplied||0)); saldo = roundCurrency(totalDevido - totalPago). Cancelled installments MUST be excluded from devido, pago and saldo (CONTEXT CRED-01: "Refletir corretamente parcelas cancelled (não somar ao saldo nem ao devido)"). Render these three figures with formatCurrency inside the existing client Card (reuse the same label/value flex-row style used at lines 421-450 for Limite/Utilizado/Disponível). Then add a per-sale breakdown: group the client's installments by saleId, and for each sale show the sale code (`saleId.slice(0,8).toUpperCase()`), its devido/pago/saldo, and a compact status count (open/overdue/paid/cancelled) reusing getStatusBadge and the existing color tokens. Keep the cancelled count visible but excluded from money sums. Use roundCurrency for all summed money values (Phase 1 helper). Do NOT introduce a new design system, table library, or component file — compose with the existing Card/Badge/grid primitives already in the file.
  </action>
  <verify>
    <automated>cd /c/dev/pdv_rcferragista && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "roundCurrency" src/pages/CreditNotes.tsx` returns >= 1 (used for the new sums).
    - The Resumo tab renders total devido, total pago and saldo for the selected client (three formatCurrency values present in the new block).
    - A per-sale breakdown groups by saleId and shows per-sale devido/pago/saldo plus a status count.
    - Cancelled installments are excluded from the money sums (grep for a filter excluding `'cancelled'` in the new aggregation, e.g. `grep -c "cancelled" src/pages/CreditNotes.tsx` increases vs. baseline / the new sum filters out cancelled).
    - `npm run build` exits 0.
    - Observable: selecting a client with one paid + one open + one cancelled installment shows saldo = sum of the open installment only, and the cancelled one does not inflate devido.
  </acceptance_criteria>
  <done>Resumo tab shows per-client devido/pago/saldo and a per-sale breakdown with status counts; cancelled installments are excluded from money sums; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| electron-store ↔ renderer | Installment records read for aggregation; display derives status without writing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | balance aggregation (devido/pago/saldo) | mitigate | Use roundCurrency on every sum to avoid centavo drift (FND-01); exclude cancelled to prevent inflated balances. |
| T-02-04 | Repudiation | on-the-fly overdue display | accept | Display-only derivation; persisted status still written by existing mount useEffect for cross-page consumers. No audit gap introduced. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No new dependency surface. |
</threat_model>

<verification>
- `npm run build` exits 0.
- CreditNotes.tsx imports and uses `getEffectiveStatus` for display and aggregation.
- Resumo tab shows per-client devido/pago/saldo + per-sale breakdown with status counts.
- Cancelled installments excluded from money sums; roundCurrency used on sums.
- An installment lapsing while the app is open shows as Vencida without reload.
</verification>

<success_criteria>
- CRED-01: per-client and per-sale total devido, total pago, saldo, and status situation are visible and correct (cancelled excluded).
- CRED-02 (display side): overdue is computed on-the-fly via the shared helper everywhere the screen renders or aggregates status.
- No new design system; existing cards/badges/formatCurrency reused.
</success_criteria>

<output>
Create `.planning/phases/02-credi-rio/02-02-SUMMARY.md` when done.
</output>
