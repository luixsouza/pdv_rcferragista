---
phase: 06-layout-fiscal
plan: "03"
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/pages/Sales.tsx
autonomous: true
requirements: [FISC-01, FISC-02]
user_setup: []

must_haves:
  truths:
    - "From the Sales history, the operator can trigger 'Gerar NFCe (cupom)' for a sale and the NFCe PDF is produced (print or download)"
    - "From the Sales history, the operator can trigger 'Gerar DANFE (NFe)' for a sale and the DANFE A4 PDF is produced (print or download)"
    - "The existing non-fiscal cupom (printReceipt/downloadReceipt) actions remain available and unchanged — the fiscal documents are additional"
    - "The matching Client (when sale.clientId is set) is passed to the generators so consumer/destinatário fields populate"
  artifacts:
    - path: "src/pages/Sales.tsx"
      provides: "NFCe and DANFE generation actions wired into the sales list and/or detail dialog"
      contains: "generateNFCe"
  key_links:
    - from: "src/pages/Sales.tsx"
      to: "src/lib/generateNFCe.ts"
      via: "import printNFCe/downloadNFCe and call onClick with the sale + matched client"
      pattern: "NFCe"
    - from: "src/pages/Sales.tsx"
      to: "src/lib/generateDANFE.ts"
      via: "import printDANFE/downloadDANFE and call onClick with the sale + matched client"
      pattern: "DANFE"
---

<objective>
Wire the NFCe and DANFE generators into Sales.tsx as additional document actions alongside the existing non-fiscal cupom, so the operator can generate either fiscal-layout document from a sale in the history list and the detail dialog.

Purpose: FISC-01 / FISC-02 success criteria require these to be reachable "a partir da tela de Vendas". This plan delivers the UI reachability.

Output: src/pages/Sales.tsx updated with NFCe + DANFE actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-layout-fiscal/06-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Plans 06-01 and 06-02 created these. -->

From src/lib/generateNFCe.ts (06-01):
```typescript
export function printNFCe(sale: Sale, client?: Client): Promise<void>;
export function downloadNFCe(sale: Sale, client?: Client): Promise<void>;
```

From src/lib/generateDANFE.ts (06-02):
```typescript
export function printDANFE(sale: Sale, client?: Client): Promise<void>;
export function downloadDANFE(sale: Sale, client?: Client): Promise<void>;
```

Existing in src/pages/Sales.tsx (already present — do NOT remove):
```typescript
import { printReceipt, downloadReceipt } from '@/lib/generateReceipt';
const [clients] = useLocalStorage<Client[]>('clients', []);  // line ~51
// list-row actions around lines 484-499; detail-dialog actions around lines 618-627
// existing print/download buttons use lucide Printer / Download icons
```
</interfaces>

<pattern_reference>
- Existing actions: `<Button variant="ghost" size="icon" onClick={() => printReceipt(sale)} title="Imprimir">`. Mirror this button style.
- The async generators return Promises; onClick handlers can call them without awaiting (fire-and-forget) like the existing sync calls, OR wrap in a small `void printNFCe(...)`. Match the project's existing fire-and-forget UI calls; surface failures with `toast.error` if a try/catch is added (toast is available via `useToast` in this file).
- Client lookup: `const client = clients.find(c => c.id === sale.clientId);` then pass to generators (consumer/destinatário fields). Pass `undefined` when no clientId.
</pattern_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NFCe + DANFE actions to Sales list rows and detail dialog</name>
  <files>src/pages/Sales.tsx</files>
  <read_first>
    - src/pages/Sales.tsx (imports ~line 41, clients state ~line 51, list-row action group ~lines 484-499, detail dialog action group ~lines 618-627)
    - .planning/phases/06-layout-fiscal/06-CONTEXT.md ("Acesso na UI": add "Gerar NFCe (cupom)" and "Gerar DANFE (NFe)" reusing print/download pattern; additional, do not replace)
    - src/lib/generateNFCe.ts and src/lib/generateDANFE.ts (the exported print/download signatures)
  </read_first>
  <action>
    In src/pages/Sales.tsx add imports: `import { printNFCe, downloadNFCe } from '@/lib/generateNFCe';` and `import { printDANFE, downloadDANFE } from '@/lib/generateDANFE';`. Add two fiscal icons from lucide-react (e.g., Receipt for NFCe, FileText for DANFE) to the existing lucide import line — keep the existing Printer/Download imports.

    Keep the existing printReceipt/downloadReceipt buttons exactly as-is (CONTEXT.md: fiscal docs are ADDITIONAL, do not replace the non-fiscal cupom).

    Add a helper at the top of the component: `const clientFor = (sale: Sale) => clients.find(c => c.id === sale.clientId);`.

    In the list-row action group (around lines 484-499): add two new ghost icon Buttons after the existing print/download buttons — "Gerar NFCe (cupom)" calling `printNFCe(sale, clientFor(sale))` (Receipt icon, title="Gerar NFCe (cupom)") and "Gerar DANFE (NFe)" calling `printDANFE(sale, clientFor(sale))` (FileText icon, title="Gerar DANFE (NFe)").

    In the detail dialog action group (around lines 618-627, where the Imprimir/Baixar PDF buttons live for selectedSale): add a second row of buttons or two outline buttons for the fiscal documents: "Gerar NFCe" -> `printNFCe(selectedSale, clientFor(selectedSale))` and "Gerar DANFE" -> `printDANFE(selectedSale, clientFor(selectedSale))`. Optionally add download variants (downloadNFCe/downloadDANFE) — at minimum print must be wired for both. Since the generators are async, calls may be fire-and-forget consistent with the existing non-awaited calls; if wrapping, use `.catch(() => toast({ ... }))` with the file's existing `toast` from useToast.

    Do not modify any unrelated logic (return flow, refund, filters).
  </action>
  <verify>
    <automated>cd C:/dev/pdv_rcferragista && grep -q "from '@/lib/generateNFCe'" src/pages/Sales.tsx && grep -q "from '@/lib/generateDANFE'" src/pages/Sales.tsx && grep -q 'printNFCe(' src/pages/Sales.tsx && grep -q 'printDANFE(' src/pages/Sales.tsx && grep -q 'printReceipt' src/pages/Sales.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Sales.tsx imports printNFCe/downloadNFCe and printDANFE/downloadDANFE
    - New action buttons call printNFCe and printDANFE with the sale and the matched client (via clients.find on sale.clientId)
    - Existing printReceipt/downloadReceipt buttons still present (non-fiscal cupom not replaced)
    - Actions appear both in the list row and the detail dialog
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Operator can generate NFCe and DANFE from the Sales screen for any sale; existing non-fiscal cupom unchanged; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sales UI -> generators | Passes local Sale + Client objects to PDF generators; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Repudiation | Operator confuses fiscal-layout doc with the real non-fiscal cupom | mitigate | Distinct button titles "Gerar NFCe (cupom)" / "Gerar DANFE (NFe)"; documents themselves carry verbatim SEM VALOR FISCAL disclaimers (FISC-03 in 06-01/06-02) |
| T-06-07 | Tampering | Accidentally removing the existing non-fiscal cupom action | accept | Acceptance criterion explicitly verifies printReceipt remains; reviewer/build grep guards it |
</threat_model>

<verification>
- `npm run build` exits 0.
- grep confirms NFCe/DANFE imports + calls and the retained printReceipt action in Sales.tsx.
</verification>

<success_criteria>
FISC-01 and FISC-02 reachability: from the Sales screen the operator generates the NFCe cupom and the DANFE A4 for any sale, with the matched client passed through, and the existing non-fiscal cupom actions remain intact.
</success_criteria>

<output>
Create `.planning/phases/06-layout-fiscal/06-03-SUMMARY.md` when done.
</output>
