---
phase: 06-layout-fiscal
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/fiscalPlaceholders.ts
  - src/lib/fiscalBarcode.ts
  - src/lib/generateNFCe.ts
autonomous: true
requirements: [FISC-01, FISC-03]
user_setup: []

must_haves:
  truths:
    - "bwip-js and qrcode appear as dependencies in package.json and the production build succeeds"
    - "Generating an NFCe from a sale produces an 80mm cupom titled exactly DOCUMENTO AUXILIAR DA NOTA FISCAL DE CONSUMIDOR ELETRÔNICA"
    - "The NFCe shows the items block, totals, payment method, the exact phrase Consulte pela Chave de Acesso em, a 44-digit placeholder access key, and a QR code image >= 25x25mm"
    - "The NFCe shows the consumer line or CONSUMIDOR NÃO IDENTIFICADO when no client is on the sale"
    - "The NFCe carries the verbatim homologation disclaimer EMITIDA EM AMBIENTE DE HOMOLOGAÇÃO – SEM VALOR FISCAL"
    - "NFCe page height grows with item count (~164 + 8*n mm) so totals never clip"
  artifacts:
    - path: "src/lib/fiscalPlaceholders.ts"
      provides: "Safe placeholder constants and access-key helper for non-transmitted fiscal docs"
      exports: ["PLACEHOLDER_ACCESS_KEY", "PLACEHOLDER_NCM", "PLACEHOLDER_CFOP", "PLACEHOLDER_CST", "PLACEHOLDER_PROTOCOL", "formatAccessKeyGroups", "NFCE_DISCLAIMER", "NFE_DISCLAIMER"]
      min_lines: 25
    - path: "src/lib/fiscalBarcode.ts"
      provides: "Async barcode (CODE-128) and QR data-URL generators wrapping bwip-js and qrcode"
      exports: ["generateBarcodeDataUrl", "generateQrDataUrl"]
      min_lines: 20
    - path: "src/lib/generateNFCe.ts"
      provides: "NFCe 80mm cupom generator + print/download wrappers"
      exports: ["generateNFCe", "printNFCe", "downloadNFCe"]
      min_lines: 120
  key_links:
    - from: "src/lib/generateNFCe.ts"
      to: "src/lib/fiscalBarcode.ts"
      via: "await generateQrDataUrl then doc.addImage"
      pattern: "generateQrDataUrl"
    - from: "src/lib/generateNFCe.ts"
      to: "src/lib/storeInfo.ts"
      via: "getStoreSettings() for emitter block"
      pattern: "getStoreSettings"
    - from: "src/lib/generateNFCe.ts"
      to: "src/lib/fiscalPlaceholders.ts"
      via: "imports access key, disclaimer, placeholder constants"
      pattern: "fiscalPlaceholders"
---

<objective>
Install the two client-side rendering dependencies (bwip-js for CODE-128 barcode, qrcode for QR), create the shared fiscal placeholder/barcode helpers, and implement the NFCe (DANFE-NFCe, 80mm) generator with QR code, dynamic height, and the verbatim homologação disclaimer.

Purpose: NFCe is the most-used document (consumer cupom) and the shared helpers it depends on (placeholders + barcode/QR data-URLs) are also consumed by the DANFE plan in wave 2. Building them here unblocks both.

Output: package.json with bwip-js + qrcode; src/lib/fiscalPlaceholders.ts; src/lib/fiscalBarcode.ts; src/lib/generateNFCe.ts (generate/print/download).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-layout-fiscal/06-CONTEXT.md
@.planning/research/FISCAL-LAYOUT.md

<interfaces>
<!-- Contracts the executor needs. Use these directly, no exploration required. -->

From src/types/index.ts:
```typescript
export interface SaleItem { productId: string; productName: string; quantity: number; unitPrice: number; costPrice?: number; total: number; }
export interface Sale {
  id: string; clientId?: string; clientName?: string; items: SaleItem[];
  subtotal: number; discount: number; total: number;
  paymentMethod: 'cash'|'credit'|'debit'|'pix'|'crediario'|'store_credit';
  paymentEntries?: { method: string; amount: number }[];
  status?: 'completed'|'refunded'|'crediario_pending'|'crediario_paid';
  createdAt: string;
}
export interface Client { id: string; name: string; document: string; address: string; city: string; }
```

From src/types/settings.ts (StoreSettings — emitter source via getStoreSettings()):
```typescript
{ storeName, cnpj, address, city, cep, phone }  // no IE field — use "ISENTO" placeholder
```

From src/lib/storeInfo.ts:
```typescript
export function getStoreSettings(): StoreSettings;  // synchronous, returns defaults on error
```

From src/lib/formatters.ts:
```typescript
export const formatCurrency: (v: number) => string;   // "R$ 1.234,56"
export const roundCurrency: (v: number) => number;     // Math.round(v*100)/100
export const paymentLabels: Record<string,string>;     // cash->Dinheiro, pix->PIX, etc.
```

From src/lib/documentValidation.ts:
```typescript
export function formatDocument(value: string): string;  // CPF or CNPJ formatting
```
</interfaces>

<pattern_reference>
The existing src/lib/generateReceipt.ts is the canonical pattern to mirror:
- `new jsPDF({ orientation:'portrait', unit:'mm', format:[80, Math.max(floor, base + items.length*8)] })`
- emitter header from getStoreSettings(), centered, helvetica bold/normal, fontSize 7-12
- `doc.line(margin,y,pageWidth-margin,y)` separators; y-cursor increments
- wrappers: `printX` -> `doc.autoPrint(); window.open(doc.output('bloburl'),'_blank')`; `downloadX` -> `doc.save(fileName)`
The dynamic-height formula and base/perItem rationale is documented in 01-02-SUMMARY.md (base + 8mm/item).
</pattern_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install bwip-js + qrcode and create fiscal helper modules</name>
  <files>package.json, package-lock.json, src/lib/fiscalPlaceholders.ts, src/lib/fiscalBarcode.ts</files>
  <read_first>
    - .planning/research/FISCAL-LAYOUT.md (Part 1.4 access key structure; Part 3 placeholder column; Part 4 verbatim legal wordings; Part 5.1/5.2 barcode/QR approach)
    - .planning/phases/06-layout-fiscal/06-CONTEXT.md (Bibliotecas novas decision; FISC-03 placeholders)
    - .planning/STATE.md (Blockers: verify bwip-js/qrcode Electron offline compatibility)
    - src/lib/storeInfo.ts (helper module shape to mirror)
  </read_first>
  <action>
    Run `npm install bwip-js qrcode` and `npm install -D @types/qrcode`. Per CONTEXT.md "Bibliotecas novas" decision both are pure client-side and must work offline at runtime (no network calls in bwip-js.toCanvas/toBuffer nor in QRCode.toDataURL). Confirm they land in package.json dependencies.

    Create src/lib/fiscalPlaceholders.ts exporting safe-placeholder constants per FISC-03 and the FISCAL-LAYOUT.md Part 3 placeholder column: PLACEHOLDER_ACCESS_KEY = 44 zero digits, PLACEHOLDER_NCM = "00000000", PLACEHOLDER_CFOP = "5102", PLACEHOLDER_CST = "400", PLACEHOLDER_PROTOCOL = "SEM PROTOCOLO", PLACEHOLDER_IE = "ISENTO", PLACEHOLDER_SERIE = "001". Export NFE_DISCLAIMER = "NF-E EMITIDA EM AMBIENTE DE HOMOLOGAÇÃO - SEM VALOR FISCAL" and NFCE_DISCLAIMER = "EMITIDA EM AMBIENTE DE HOMOLOGAÇÃO – SEM VALOR FISCAL" (note the en-dash and accents in NFCE per FISCAL-LAYOUT.md Part 4 — copy verbatim, do not normalize the dash). Export formatAccessKeyGroups(key: string): string that splits a 44-char string into 11 groups of 4 separated by single spaces. Export a NFCE_QR_PLACEHOLDER url string containing the access key and sem_valor_fiscal=1 marker (FISCAL-LAYOUT.md 2.3 Divisão V placeholder). Use named exports, camelCase functions, SCREAMING_SNAKE_CASE constants per project conventions.

    Create src/lib/fiscalBarcode.ts. Export `async function generateQrDataUrl(text: string): Promise<string>` calling qrcode's toDataURL(text, { width: 94, margin: 1 }) and returning the PNG data URL. Export `async function generateBarcodeDataUrl(text: string): Promise<string>` using bwip-js to render a CODE-128 barcode of `text` to a canvas/buffer and return a PNG data URL; use the browser/canvas entry point appropriate for the Electron renderer (bwip-js toCanvas on an offscreen document.createElement('canvas') then canvas.toDataURL('image/png'), or toBuffer base64). Both functions must avoid any network access. Wrap bwip-js import to be compatible with the project's bundler/module resolution (default import, then call the canvas API).

    Do NOT inline these helpers into the generators — they are shared with the DANFE plan in wave 2.
  </action>
  <verify>
    <automated>cd C:/dev/pdv_rcferragista && grep -q '"bwip-js"' package.json && grep -q '"qrcode"' package.json && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains both "bwip-js" and "qrcode" under dependencies
    - src/lib/fiscalPlaceholders.ts exports PLACEHOLDER_ACCESS_KEY (44 zeros), NFE_DISCLAIMER, NFCE_DISCLAIMER (verbatim with en-dash), formatAccessKeyGroups
    - src/lib/fiscalBarcode.ts exports async generateQrDataUrl and generateBarcodeDataUrl returning PNG data URLs with no network calls
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Both deps installed and present in package.json; both helper modules exist with the listed exports; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement generateNFCe (80mm cupom) with QR, dynamic height, disclaimer</name>
  <files>src/lib/generateNFCe.ts</files>
  <read_first>
    - src/lib/generateReceipt.ts (the 80mm jsPDF pattern, dynamic-height formula, print/download wrappers to mirror exactly)
    - .planning/research/FISCAL-LAYOUT.md (Part 2: Divisões I–IX order, Part 2.4 coordinate/height reference 164 + 8n, Part 4 NFCe verbatim wordings)
    - .planning/phases/06-layout-fiscal/06-CONTEXT.md (FISC-01 cupom spec, FISC-03 disclaimer)
    - .planning/phases/01-funda-o-e-pdf/01-02-SUMMARY.md (dynamic-height rationale: base + 8mm/item, Math.max floor)
    - src/lib/formatters.ts (formatCurrency, paymentLabels), src/lib/documentValidation.ts (formatDocument), src/lib/storeInfo.ts (getStoreSettings)
  </read_first>
  <action>
    Create src/lib/generateNFCe.ts exporting `async function generateNFCe(sale: Sale, client?: Client): Promise<jsPDF>`. It must be async because the QR data URL from generateQrDataUrl is awaited before addImage. Mirror generateReceipt.ts structure for the 80mm layout and dynamic height: set format [80, Math.max(250, 164 + sale.items.length * 8)] per FISCAL-LAYOUT.md Part 2.4 estimated total (164 + 8n) — keep a Math.max floor so small cupons retain a sane minimum.

    Render the FISCAL-LAYOUT.md Part 2.3 divisões in order using getStoreSettings() for the emitter:
    - Divisão I: store name (bold), CNPJ, IE (PLACEHOLDER_IE "ISENTO"), address, city, CEP, phone — centered.
    - Divisão II: separator then centered bold title verbatim "DOCUMENTO AUXILIAR DA NOTA FISCAL DE CONSUMIDOR ELETRÔNICA" (FISC-01 requires this exact string).
    - Disclaimer band (FISC-03): centered bold NFCE_DISCLAIMER verbatim ("EMITIDA EM AMBIENTE DE HOMOLOGAÇÃO – SEM VALOR FISCAL"); draw a filled rect band or bold highlighted text so it is prominent.
    - Divisão III: item header line, then per item two lines (name; then qty/un x unitPrice = total) using formatCurrency for money; truncate long names like generateReceipt does.
    - Divisão III-A: item count, subtotal, desconto (if >0), bold VALOR TOTAL R$; then payment method via paymentLabels (handle paymentEntries multi-line like generateReceipt).
    - Divisão IV: separator, verbatim "Consulte pela Chave de Acesso em" (FISC-01 exact phrase), a SEFAZ-style URL line, then formatAccessKeyGroups(PLACEHOLDER_ACCESS_KEY) printed as 11 groups of 4.
    - Divisão V: await generateQrDataUrl(NFCE_QR_PLACEHOLDER) and doc.addImage(dataUrl,'PNG',x,y,25,25) centered — at least 25x25mm per FISC-01.
    - Divisão VI: if sale.clientName/client present, print "CONSUMIDOR" + name + formatDocument(client.document); else print "CONSUMIDOR NÃO IDENTIFICADO".
    - Divisão VII: NFC-e nº (sale.id.slice(0,9)), Série PLACEHOLDER_SERIE, emissão sale.createdAt formatted, PROTOCOLO DE AUTORIZAÇÃO: PLACEHOLDER_PROTOCOL.
    - Divisão VIII: repeat NFCE_DISCLAIMER centered bold.

    Export `async function printNFCe(sale, client?)` -> `const doc = await generateNFCe(...); doc.autoPrint(); window.open(doc.output('bloburl'),'_blank')` and `async function downloadNFCe(sale, client?)` -> `const doc = await generateNFCe(...); doc.save('nfce_'+sale.id.slice(0,8)+'.pdf')`. Use roundCurrency where totals are recomputed; otherwise read sale.subtotal/discount/total directly. No fenced code in the file beyond normal TS.
  </action>
  <verify>
    <automated>cd C:/dev/pdv_rcferragista && grep -q 'DOCUMENTO AUXILIAR DA NOTA FISCAL DE CONSUMIDOR ELETRÔNICA' src/lib/generateNFCe.ts && grep -q 'Consulte pela Chave de Acesso em' src/lib/generateNFCe.ts && grep -q 'addImage' src/lib/generateNFCe.ts && grep -Eq '164 \+|\* 8' src/lib/generateNFCe.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - generateNFCe.ts contains the verbatim title "DOCUMENTO AUXILIAR DA NOTA FISCAL DE CONSUMIDOR ELETRÔNICA"
    - Contains verbatim "Consulte pela Chave de Acesso em" and references NFCE_DISCLAIMER / the disclaimer string
    - Calls doc.addImage with the awaited QR data URL (QR rendered, >=25mm size argument)
    - Dynamic height present: format uses 164 + items.length*8 with a Math.max floor
    - Branch prints "CONSUMIDOR NÃO IDENTIFICADO" when no client
    - Exports generateNFCe (async), printNFCe, downloadNFCe; build exits 0
  </acceptance_criteria>
  <done>NFCe cupom generates with the official title, access-key phrase + placeholder key, QR image, consumer line/fallback, disclaimer, and dynamic height; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install -> bundled renderer | New packages (bwip-js, qrcode) execute in the Electron renderer; supply-chain risk |
| Sale/StoreSettings data -> PDF text | Local trusted data rendered to PDF; no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | Generated NFCe cupom mistaken for authorized fiscal doc | mitigate | Verbatim NFCE_DISCLAIMER band + Divisão VIII repeat; PLACEHOLDER_PROTOCOL "SEM PROTOCOLO"; all-zero access key (FISC-03) |
| T-06-02 | Tampering | npm install of bwip-js + qrcode | mitigate | slopcheck + legitimacy checkpoint (T-06-SC) before relying on them |
| T-06-03 | Information disclosure | QR payload leaking real data | accept | QR encodes only a static placeholder URL with zero access key + sem_valor_fiscal=1; no PII |
| T-06-SC | Tampering | bwip-js, qrcode, @types/qrcode installs | mitigate | Package legitimacy verified (both are widely-used MIT packages, present in RESEARCH.md 5.6); verify on npmjs.com before install |
</threat_model>

<verification>
- `npm run build` exits 0 with both new deps installed.
- grep confirms verbatim NFCe title, access-key phrase, disclaimer string, addImage (QR), and dynamic-height arithmetic in generateNFCe.ts.
- Helper modules export the documented symbols.
</verification>

<success_criteria>
FISC-01 (NFCe layout) and FISC-03 (disclaimer + placeholders, NFCe side) are satisfied: an NFCe cupom is generated from a Sale with the official title, items/totals/payment, access-key phrase + 44-digit placeholder key, QR >=25mm, consumer line or "CONSUMIDOR NÃO IDENTIFICADO", verbatim homologação disclaimer, and dynamic height. Shared placeholder/barcode helpers exist for the DANFE plan.
</success_criteria>

<output>
Create `.planning/phases/06-layout-fiscal/06-01-SUMMARY.md` when done.
</output>
