---
phase: 06-layout-fiscal
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/lib/generateDANFE.ts
autonomous: true
requirements: [FISC-02, FISC-03]
user_setup: []

must_haves:
  truths:
    - "Generating a DANFE from a sale produces an A4 (210x297mm) PDF with the 8 mandatory quadros in canonical order"
    - "The product table renders fiscal columns NCM/CST/CFOP/Un/Qtd/VlUnit/VlTotal/BC ICMS/Alíq ICMS/Vl ICMS/Vl IPI, present but zeroed via placeholders"
    - "The tax-calc block (Cálculo do Imposto) renders with bases/values zeroed and Valor Total da NF from sale.total"
    - "A CODE-128 barcode of the 44-digit placeholder access key renders in the header area"
    - "When the product table overflows a page, content paginates to a new A4 page without clipping the totals/tax block"
    - "The DANFE carries the verbatim disclaimer NF-E EMITIDA EM AMBIENTE DE HOMOLOGAÇÃO - SEM VALOR FISCAL"
  artifacts:
    - path: "src/lib/generateDANFE.ts"
      provides: "DANFE NFe A4 generator + print/download wrappers"
      exports: ["generateDANFE", "printDANFE", "downloadDANFE"]
      min_lines: 180
  key_links:
    - from: "src/lib/generateDANFE.ts"
      to: "src/lib/fiscalBarcode.ts"
      via: "await generateBarcodeDataUrl then doc.addImage"
      pattern: "generateBarcodeDataUrl"
    - from: "src/lib/generateDANFE.ts"
      to: "src/lib/fiscalPlaceholders.ts"
      via: "NCM/CFOP/CST/access-key/disclaimer placeholders"
      pattern: "PLACEHOLDER_|NFE_DISCLAIMER"
    - from: "src/lib/generateDANFE.ts"
      to: "src/lib/storeInfo.ts"
      via: "getStoreSettings() for emitter quadro"
      pattern: "getStoreSettings"
---

<objective>
Implement the DANFE (NFe model 55) A4 generator: 8 mandatory quadros in canonical order, the fiscal product table with all fiscal columns (zeroed via placeholders), the tax-calculation block, the CODE-128 barcode of the placeholder access key, page pagination for long item lists, and the verbatim homologação disclaimer.

Purpose: FISC-02 — the A4 fiscal document. Depends on the shared placeholder/barcode helpers from plan 06-01.

Output: src/lib/generateDANFE.ts (generate/print/download).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-layout-fiscal/06-CONTEXT.md
@.planning/research/FISCAL-LAYOUT.md

<interfaces>
<!-- Contracts the executor needs. Plan 06-01 created the helpers below. -->

From src/lib/fiscalPlaceholders.ts (created in 06-01):
```typescript
export const PLACEHOLDER_ACCESS_KEY: string;   // 44 zeros
export const PLACEHOLDER_NCM: string;          // "00000000"
export const PLACEHOLDER_CFOP: string;         // "5102"
export const PLACEHOLDER_CST: string;          // "400"
export const PLACEHOLDER_PROTOCOL: string;     // "SEM PROTOCOLO"
export const PLACEHOLDER_IE: string;           // "ISENTO"
export const PLACEHOLDER_SERIE: string;        // "001"
export const NFE_DISCLAIMER: string;           // "NF-E EMITIDA EM AMBIENTE DE HOMOLOGAÇÃO - SEM VALOR FISCAL"
export function formatAccessKeyGroups(key: string): string;  // 11 groups of 4
```

From src/lib/fiscalBarcode.ts (created in 06-01):
```typescript
export function generateBarcodeDataUrl(text: string): Promise<string>;  // CODE-128 PNG data URL
```

From src/types/index.ts:
```typescript
export interface SaleItem { productId: string; productName: string; quantity: number; unitPrice: number; total: number; }
export interface Sale { id: string; clientId?: string; clientName?: string; items: SaleItem[]; subtotal: number; discount: number; total: number; createdAt: string; }
export interface Client { id: string; name: string; document: string; address: string; city: string; }
```

From src/lib/storeInfo.ts: `getStoreSettings(): StoreSettings` (storeName, cnpj, address, city, cep, phone; no IE).
From src/lib/formatters.ts: `formatCurrency`, `roundCurrency`.
From src/lib/documentValidation.ts: `formatDocument(value: string): string`.
</interfaces>

<pattern_reference>
- jsPDF A4 constructor per FISCAL-LAYOUT.md 5.3: `new jsPDF({ orientation:'portrait', unit:'mm', format:'a4' })` (210x297).
- Bordered quadros via `doc.rect(x,y,w,h)` with `doc.setDrawColor(0)` + `doc.setLineWidth(0.3)`; small bold (6-7pt) labels top-left inside each box, value below/beside (FISCAL-LAYOUT.md 5.4).
- New page: `doc.addPage('a4','portrait')` and reset the y-cursor; repeat barcode/access-key header per FISCAL-LAYOUT.md 1.1/1.5.
- Wrappers mirror generateReceipt.ts: printDANFE -> autoPrint + window.open(bloburl); downloadDANFE -> doc.save(fileName).
</pattern_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the DANFE A4 quadros 0-3 + header barcode + disclaimer</name>
  <files>src/lib/generateDANFE.ts</files>
  <read_first>
    - .planning/research/FISCAL-LAYOUT.md (Part 1.3 quadro order/labels, 1.4 access key, 1.5 coordinate reference Y-table, Part 4 DANFE verbatim wordings, 5.1 barcode placement, 5.4 drawing quadros)
    - .planning/phases/06-layout-fiscal/06-CONTEXT.md (FISC-02 8-quadros spec, FISC-03 disclaimer)
    - src/lib/generateReceipt.ts (jsPDF/print/download pattern to mirror)
    - src/lib/storeInfo.ts (getStoreSettings), src/lib/documentValidation.ts (formatDocument)
  </read_first>
  <action>
    Create src/lib/generateDANFE.ts exporting `async function generateDANFE(sale: Sale, client?: Client): Promise<jsPDF>` (async to await the barcode data URL). Construct the A4 doc per FISCAL-LAYOUT.md 5.3 with 7mm margins (working width 196mm). Use the Y-table in FISCAL-LAYOUT.md 1.5 as the coordinate baseline (exact coords are Claude's discretion per CONTEXT.md).

    Draw, in order, using doc.rect for borders and 6-7pt bold labels (FISCAL-LAYOUT.md 5.4):
    - Disclaimer band (FISC-03): filled dark rect ~6mm tall at top with centered bold white verbatim NFE_DISCLAIMER ("NF-E EMITIDA EM AMBIENTE DE HOMOLOGAÇÃO - SEM VALOR FISCAL"). Optionally also a faint diagonal "SEM VALOR FISCAL" watermark over the body (discretion).
    - Quadro 0 (header): left column "IDENTIFICAÇÃO DO EMITENTE" + getStoreSettings() razão social/endereço/CEP/município/UF/fone/CNPJ/IE(PLACEHOLDER_IE); center "DANFE" large bold + "DOCUMENTO AUXILIAR DA NOTA FISCAL ELETRÔNICA" + "1 - SAÍDA" + Nº (sale.id.slice(0,9)) / Série PLACEHOLDER_SERIE; right: await generateBarcodeDataUrl(PLACEHOLDER_ACCESS_KEY) -> doc.addImage horizontal in the header, label "CHAVE DE ACESSO", formatAccessKeyGroups(PLACEHOLDER_ACCESS_KEY), and the "Consulte a autenticidade no portal nacional da NF-e www.nfe.fazenda.gov.br" line.
    - Quadro 1 (Natureza/Protocolo): "NATUREZA DA OPERAÇÃO" = "VENDA DE MERCADORIAS A CONSUMIDOR"; "PROTOCOLO DE AUTORIZAÇÃO DE USO" = PLACEHOLDER_PROTOCOL; second row INSCRIÇÃO ESTADUAL (PLACEHOLDER_IE) / INS.EST.SUBST.TRIB (blank) / CNPJ (store).
    - Quadro 2 (Destinatário/Remetente): header "DESTINATÁRIO / REMETENTE"; NOME/RAZÃO SOCIAL = client?.name || "CONSUMIDOR"; CNPJ/CPF via formatDocument(client?.document); DATA DA EMISSÃO from sale.createdAt; ENDEREÇO/BAIRRO/CEP/MUNICÍPIO/UF/FONE/IE rows (blank when no client).
    - Quadro 3 (Fatura): header "FATURA" left blank (retail cash sale per FISCAL-LAYOUT.md 1.3 Quadro 3).

    Keep the running y-cursor and expose a helper to draw the repeating header (disclaimer band + barcode strip) so Task 2 can reuse it on overflow pages. Do not implement the product table or tax block here — that is Task 2. Build must pass after this task even though the lower quadros are stubbed/TODO-commented as long as TS compiles; prefer to land Task 1 + Task 2 together if a partial file would not compile.
  </action>
  <verify>
    <automated>cd C:/dev/pdv_rcferragista && grep -q 'NF-E EMITIDA EM AMBIENTE DE HOMOLOGAÇÃO - SEM VALOR FISCAL' src/lib/generateDANFE.ts && grep -q 'IDENTIFICAÇÃO DO EMITENTE' src/lib/generateDANFE.ts && grep -q 'generateBarcodeDataUrl' src/lib/generateDANFE.ts && grep -q 'DESTINATÁRIO / REMETENTE' src/lib/generateDANFE.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - generateDANFE.ts constructs an A4 jsPDF (format 'a4')
    - Contains verbatim NFE_DISCLAIMER, "IDENTIFICAÇÃO DO EMITENTE", "DANFE", "DESTINATÁRIO / REMETENTE", "NATUREZA DA OPERAÇÃO"
    - Awaits generateBarcodeDataUrl(PLACEHOLDER_ACCESS_KEY) and doc.addImage for the barcode
    - A reusable repeat-header helper exists for overflow pages
    - build exits 0
  </acceptance_criteria>
  <done>Quadros 0-3, header barcode, and disclaimer band render on A4; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Product table (fiscal columns), tax-calc block, quadros 6-7, pagination, wrappers</name>
  <files>src/lib/generateDANFE.ts</files>
  <read_first>
    - .planning/research/FISCAL-LAYOUT.md (Quadro 4 column table cols 1-14, Quadro 5 Cálculo do Imposto labels, Quadro 6/7 labels, 1.1 multi-page rule, 1.5 overflow rule, 3.1 data-vs-placeholder mapping)
    - .planning/phases/06-layout-fiscal/06-CONTEXT.md (FISC-02 fiscal columns zeroed but present; paginate, do not clip totals)
    - src/lib/generateReceipt.ts (item loop + print/download wrappers)
    - the Task 1 portion of src/lib/generateDANFE.ts (reuse the repeat-header helper + y-cursor)
  </read_first>
  <action>
    Continue src/lib/generateDANFE.ts after the Task 1 quadros.

    Quadro 4 "DADOS DO PRODUTO / SERVIÇO": draw a column header row with the fiscal columns from FISCAL-LAYOUT.md 1.3 Quadro 4: CÓDIGO, DESCRIÇÃO, NCM, CST, CFOP, UN, QTD, VL.UNIT, VL.TOTAL, BC ICMS, ALÍQ ICMS, VL ICMS, VL IPI (CONTEXT.md FISC-02 lists exactly these). Per item, map per FISCAL-LAYOUT.md 3.1: CÓDIGO = item.productId.slice(0,8); DESCRIÇÃO = item.productName (truncate to column width); NCM = PLACEHOLDER_NCM; CST = PLACEHOLDER_CST; CFOP = PLACEHOLDER_CFOP; UN from item (fallback "UN"); QTD = item.quantity; VL.UNIT = formatCurrency(item.unitPrice); VL.TOTAL = formatCurrency(item.total); BC ICMS / ALÍQ / VL ICMS / VL IPI = "0,00" (zeroed but present per FISC-02/FISC-03). Use small font (6-7pt) so 13 columns fit the 196mm width.

    Pagination (FISC-02 "paginate if overflows, don't clip totals"; FISCAL-LAYOUT.md 1.1/1.5): track y; before drawing each item row, if y + rowHeight would exceed the page body bottom (leave room only when there is no more space), call doc.addPage, invoke the repeat-header helper from Task 1 (disclaimer band + barcode strip), add "Folha X/Y" page numbering, and continue the table. The tax/totals block must never be clipped — if it doesn't fit after the last item, start it on a fresh page.

    Quadro 5 "CÁLCULO DO IMPOSTO": labeled boxes per FISCAL-LAYOUT.md Quadro 5 — BASE DE CÁLCULO DO ICMS "0,00", VALOR DO ICMS "0,00", BASE ICMS ST "0,00", VALOR ICMS ST "0,00", VALOR TOTAL DOS PRODUTOS = sum of item.total (roundCurrency), VALOR DO FRETE/SEGURO/OUTRAS "0,00", DESCONTO = formatCurrency(sale.discount), VALOR DO IPI "0,00", and VALOR TOTAL DA NF = formatCurrency(sale.total) (bold, prominent).
    Quadro 6 "TRANSPORTADOR / VOLUMES TRANSPORTADOS": header + FRETE POR CONTA = "9-SEM FRETE", remaining fields blank.
    Quadro 7 "DADOS ADICIONAIS": "INFORMAÇÕES COMPLEMENTARES" = "Documento sem valor fiscal. Não autorizado na SEFAZ." + repeat the disclaimer text; "RESERVADO AO FISCO" blank.

    Export `async function printDANFE(sale, client?)` -> `const doc = await generateDANFE(...); doc.autoPrint(); window.open(doc.output('bloburl'),'_blank')` and `async function downloadDANFE(sale, client?)` -> `const doc = await generateDANFE(...); doc.save('danfe_'+sale.id.slice(0,8)+'.pdf')`.
  </action>
  <verify>
    <automated>cd C:/dev/pdv_rcferragista && grep -q 'DADOS DO PRODUTO / SERVIÇO' src/lib/generateDANFE.ts && grep -q 'CÁLCULO DO IMPOSTO' src/lib/generateDANFE.ts && grep -q 'TRANSPORTADOR / VOLUMES TRANSPORTADOS' src/lib/generateDANFE.ts && grep -q 'addPage' src/lib/generateDANFE.ts && grep -Eq 'PLACEHOLDER_NCM|PLACEHOLDER_CFOP|PLACEHOLDER_CST' src/lib/generateDANFE.ts && grep -q 'downloadDANFE' src/lib/generateDANFE.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Product table header includes NCM, CST, CFOP and money columns; per-item rows use PLACEHOLDER_NCM/CFOP/CST and zeroed tax columns
    - "CÁLCULO DO IMPOSTO" block present with VALOR TOTAL DA NF from sale.total and zeroed bases
    - "TRANSPORTADOR / VOLUMES TRANSPORTADOS" and "DADOS ADICIONAIS" quadros present (all 8 quadros total)
    - doc.addPage pagination path exists with repeated header and page numbering; totals block not clipped
    - Exports generateDANFE (async), printDANFE, downloadDANFE; build exits 0
  </acceptance_criteria>
  <done>All 8 quadros render, fiscal columns present and zeroed, barcode in header, table paginates without clipping totals; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sale/StoreSettings data -> A4 PDF | Local trusted data; no untrusted input |
| Placeholder fiscal values -> printed doc | Must be unmistakably non-authorized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Spoofing | DANFE A4 mistaken for authorized NFe | mitigate | Verbatim NFE_DISCLAIMER band + "RESERVADO AO FISCO"/INFORMAÇÕES COMPLEMENTARES non-fiscal note; PLACEHOLDER_PROTOCOL; all-zero barcode/access key (FISC-03) |
| T-06-05 | Denial of service | Many-item sale overflowing one page and clipping totals | mitigate | Pagination via addPage with repeated header; tax/totals block forced onto a fresh page if it would not fit (FISC-02) |
| T-06-SC | Tampering | bwip-js/qrcode (consumed via 06-01) | mitigate | Legitimacy verified in plan 06-01 (T-06-SC); no new installs here |
</threat_model>

<verification>
- `npm run build` exits 0.
- grep confirms all 8 quadro headers/labels, fiscal column placeholders, barcode call, addPage pagination, verbatim NFE_DISCLAIMER, and the export wrappers in generateDANFE.ts.
</verification>

<success_criteria>
FISC-02 (DANFE A4 layout) and FISC-03 (disclaimer + placeholders, NFe side) are satisfied: a DANFE A4 is generated from a Sale with the 8 quadros in order, the fiscal product table (columns present, zeroed), the tax-calc block, the CODE-128 barcode of the placeholder key, page pagination that never clips totals, and the verbatim homologação disclaimer.
</success_criteria>

<output>
Create `.planning/phases/06-layout-fiscal/06-02-SUMMARY.md` when done.
</output>
