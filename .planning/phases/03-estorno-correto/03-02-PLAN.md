---
phase: 03-estorno-correto
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/pages/Sales.tsx
  - src/pages/Reports.tsx
autonomous: true
requirements: [EST-01, EST-02, EST-03, EST-04]
must_haves:
  truths:
    - "Estorno de crediário com zero pago cancela parcelas open/overdue e NÃO gera nenhum haver"
    - "Estorno de crediário com valor pago > 0 abre diálogo perguntando haver ou dinheiro"
    - "Opção haver adiciona exatamente roundCurrency(pago) ao client.storeCredit; opção dinheiro não toca storeCredit e registra saída de caixa"
    - "Estorno de venda mista mostra o valor total pago (parcelas pagas + entrada/parcela em dinheiro) e sinaliza a porção em dinheiro ao operador"
    - "A venda estornada grava cancelledInstallmentIds com os ids das parcelas canceladas (auditoria EST-02 / base reversão Fase 4)"
    - "Estorno após devolução parcial não restaura estoque em dobro"
    - "Parcelas pagas (incl. entrada number:0) nunca são canceladas no estorno"
    - "Fechamento de caixa (Reports.tsx) reflete a saída de caixa do estorno em dinheiro, distinta do total de Estornos"
  artifacts:
    - path: "src/pages/Sales.tsx"
      provides: "handleRefund wired to processRefund + operator haver/cash dialog (EST-03) + persists cancelledInstallmentIds on the refunded sale"
      contains: "processRefund"
    - path: "src/pages/Reports.tsx"
      provides: "Cash-out (saída de caixa) line in fechamento de caixa from Sale.cashRefundOut, clearly distinct from the accounting Estornos total"
      contains: "cashRefundOut"
  key_links:
    - from: "src/pages/Sales.tsx"
      to: "src/lib/processRefund.ts"
      via: "import + call in handleRefund"
      pattern: "processRefund"
    - from: "src/pages/Sales.tsx"
      to: "client.storeCredit"
      via: "haver path increments storeCredit by roundCurrency(paidAmount)"
      pattern: "storeCredit"
    - from: "src/pages/Sales.tsx"
      to: "Sale.cancelledInstallmentIds"
      via: "finalizeRefund persists result.cancelledInstallmentIds on the refunded sale in all modes"
      pattern: "cancelledInstallmentIds"
    - from: "src/pages/Reports.tsx"
      to: "Sale.cashRefundOut"
      via: "sum cashRefundOut across day refunded sales"
      pattern: "cashRefundOut"
---

<objective>
Wire the estorno flow in Sales.tsx to the pure processRefund core, add the operator haver-vs-cash decision dialog (with mixed-payment awareness), persist the cancelled-installment audit trail on the sale, and reflect the cash-out in the fechamento de caixa report.

Purpose: With the correct arithmetic available (Plan 01), replace the buggy body of `handleRefund` so estorno produces the exact financial effect: no haver for unpaid debt (EST-01), automatic cancellation of only open/overdue installments recorded for audit on the sale (EST-02), an operator prompt for haver-vs-cash when money was actually paid — including the full mixed/split paid amount (EST-03), and double-restock-safe stock restoration (EST-04). The cash-refund-out branch must be visible and unambiguous in the daily cash close.

Output: corrected `handleRefund` + estorno dialog in Sales.tsx (persisting cancelledInstallmentIds); cash-out line in Reports.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-estorno-correto/03-CONTEXT.md
@.planning/research/FINANCIAL-PITFALLS.md
@.planning/phases/03-estorno-correto/03-01-PLAN.md

<interfaces>
<!-- Contracts the executor needs. From Plan 01 + existing codebase. -->

From src/lib/processRefund.ts (created in Plan 01):
```typescript
interface ProcessRefundInput { sale: Sale; products: Product[]; installments: Installment[]; alreadyReturnedQtys: Record<string, number>; }
interface ProcessRefundResult {
  updatedProducts: Product[];
  cancelledInstallmentIds: string[];
  paidAmount: number;      // full amount actually paid (crediarioPaid + otherPaid) for crediário; sale.total for non-crediário
  crediarioPaid: number;   // sum of installment.amountPaid for this sale (incl. entrada number:0)
  otherPaid: number;       // sum of non-crediário paymentEntries (cash/card/pix entry)
  isCrediarioSale: boolean;
}
function processRefund(input: ProcessRefundInput): ProcessRefundResult;
```

From src/pages/Sales.tsx (current state — what to modify):
```typescript
// hooks at top: sales,setSales / products,setProducts / installments,setInstallments / clients,setClients / returns,setReturns / creditPayments (read-only, line 50)
// getReturnedQuantities(saleId): Record<string,number>  // line 200 — reuse for alreadyReturnedQtys
// handleRefund(sale: Sale)  // lines 115-165 — REPLACE body
// estorno AlertDialog  // lines 600-656 (inside selectedSale dialog) — replace inline handleRefund onClick with the two-step decision flow
// formatCurrency, roundCurrency from '@/lib/formatters' (roundCurrency NOT yet imported here — add it)
```

From src/types/index.ts (after Plan 01):
```typescript
// Sale.cashRefundOut?: number  — set on the refunded sale when cash-out path chosen
// Sale.cancelledInstallmentIds?: string[]  — set on the refunded sale in ALL finalizeRefund modes
// Client.storeCredit?: number  — haver path increments this
```

From src/pages/Reports.tsx (current state):
```typescript
// cashReport useMemo (lines 27-70): dayRefunds = sales.filter(s => s.status==='refunded' && day match); totalRefunds sum of s.total
// render fechamento (lines 200-256): Estornos card (line 225-230); Formas de Pagamento card (233-255)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite handleRefund, add operator haver-vs-cash dialog, persist cancelledInstallmentIds (EST-01..04)</name>
  <files>src/pages/Sales.tsx</files>
  <read_first>
    - src/pages/Sales.tsx FULL FILE (handleRefund 115-165; getReturnedQuantities 200-210; estorno AlertDialog 600-656; existing AlertDialog/Dialog imports lines 19-36; toast usage)
    - src/lib/processRefund.ts (the Plan 01 contract — input/output shape incl. crediarioPaid, otherPaid)
    - src/lib/formatters.ts (roundCurrency, formatCurrency, paymentLabels)
    - .planning/phases/03-estorno-correto/03-CONTEXT.md (EST-03 decision: haver requires a client; no client → cash only; cash path records saída de caixa, no storeCredit)
    - .planning/research/FINANCIAL-PITFALLS.md §1 (mixed/split payment — cash portion returned as cash) and §5 edge-case table (E1,E3,E4,E5,I1,I3,I5,I6 are the acceptance scenarios)
  </read_first>
  <behavior>
    - Scenario E1/E5/I5 (crediário, zero paid): processRefund returns paidAmount 0 → cancel open/overdue installments via cancelledInstallmentIds, mark sale refunded, persist cancelledInstallmentIds on the sale, restore stock; NO haver, NO cash-out, NO dialog. Toast: "Venda estornada. Parcelas canceladas. Nenhum valor pago — nenhum haver gerado.".
    - Scenario E3 (crediário, partial paid, paidAmount > 0): open dialog asking "Devolver R$ {paidAmount} como Haver ou Dinheiro?". When result.otherPaid > 0, the dialog adds a hint that R$ {otherPaid} was paid in cash/entry and is naturally a cash disbursement (so the operator understands the haver-vs-cash choice covers the full paid amount, not just the crediário fraction). Haver option disabled/hidden when sale has no clientId.
    - Scenario E4 (crediário, fully paid / crediario_paid): paidAmount = sum of all installments.amountPaid (+ any non-crediário paymentEntries); dialog shows that full paidAmount; haver adds exactly roundCurrency(paidAmount) to storeCredit, or operator chooses cash.
    - Haver chosen: client.storeCredit += roundCurrency(paidAmount) on sale.clientId; do NOT set cashRefundOut; toast mentions haver of roundCurrency(paidAmount).
    - Cash chosen: do NOT touch storeCredit; set sale.cashRefundOut = roundCurrency(paidAmount) on the refunded sale; toast mentions saída de caixa.
    - In ALL modes ('haver' | 'cash' | 'none'): persist sale.cancelledInstallmentIds = result.cancelledInstallmentIds on the refunded sale record (audit trail for EST-02 and basis for Phase 4 DEV-07 reversal).
    - Non-crediário sale (cash/card/pix): existing behavior preserved — full reversal, mark refunded, restore stock (double-restock-safe), persist cancelledInstallmentIds (empty array is fine). No haver/cash dialog needed (paidAmount = total, but storeCredit was never used for these; keep current "estoque atualizado" toast). Do NOT regress E-type cash-sale behavior.
    - Installment cancellation: only ids in cancelledInstallmentIds flip to 'cancelled'; map (not filter) so re-run is idempotent; paid/entry-number-0 untouched (I1, I6).
    - Stock: use processRefund.updatedProducts (already double-restock-safe with mil rule) — I3.
    - Idempotency guard: if sale.status === 'refunded', return early (defensive).
  </behavior>
  <action>
    Import processRefund from '@/lib/processRefund' and add roundCurrency to the existing '@/lib/formatters' import. Rewrite handleRefund(sale) to: (1) early-return if sale.status === 'refunded'; (2) call processRefund({ sale, products, installments, alreadyReturnedQtys: getReturnedQuantities(sale.id) }); (3) setProducts(result.updatedProducts); (4) setInstallments(installments.map(inst => result.cancelledInstallmentIds.includes(inst.id) ? { ...inst, status: 'cancelled' as const } : inst)). Add a helper finalizeRefund(sale, result, mode: 'haver' | 'cash' | 'none'): for 'haver' increment the matching client's storeCredit by roundCurrency(result.paidAmount) and updatedAt; for 'cash' set the refunded sale's cashRefundOut = roundCurrency(result.paidAmount); for 'none' neither. In ALL three modes, the setSales(sales.map(...)) call that marks the sale refunded must ALSO set cancelledInstallmentIds: result.cancelledInstallmentIds on that refunded sale object (single setSales update per mode, carrying status 'refunded', cancelledInstallmentIds, and cashRefundOut when mode === 'cash'). Wire the UI: when the operator confirms estorno, compute result first; if result.isCrediarioSale && result.paidAmount > 0 open a NEW decision dialog (reuse AlertDialog or Dialog already imported) offering "Gerar Haver" (disabled when !sale.clientId) and "Devolver em Dinheiro"; the dialog body shows formatCurrency(result.paidAmount), and when result.otherPaid > 0 also shows a hint line "Inclui R$ {formatCurrency(result.otherPaid)} pago em dinheiro/entrada". Selecting either calls finalizeRefund with the chosen mode. If result.paidAmount === 0 call finalizeRefund with mode 'none' (crediário zero-paid); for non-crediário call finalizeRefund with mode 'cash' or 'none' preserving the existing non-crediário toast (storeCredit untouched). Use a small piece of state (e.g., pendingRefund: { sale, result } | null) to drive the decision dialog. Keep toasts via the existing useToast hook. Do NOT introduce new storage keys. Money everywhere via roundCurrency; display via formatCurrency. No fenced code blocks in JSX beyond normal TSX.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Sales.tsx imports processRefund (grep -n "processRefund" matches import + call).
    - handleRefund no longer restores stock with the naive `product.stock + saleItem.quantity` loop (grep: the old `stock: product.stock + saleItem.quantity` line is gone).
    - Installment cancellation uses cancelledInstallmentIds.includes (grep matches), NOT the old blanket `inst.saleId === sale.id ? cancelled` map.
    - finalizeRefund persists cancelledInstallmentIds on the refunded sale in ALL modes (grep -n "cancelledInstallmentIds" matches inside the setSales update; source assertion: the refunded-sale object carries cancelledInstallmentIds: result.cancelledInstallmentIds in the 'haver', 'cash', and 'none' paths).
    - storeCredit is incremented by roundCurrency(...paidAmount) only on the haver branch (source assertion: storeCredit add sits inside the 'haver' mode path).
    - cashRefundOut is set on the refunded sale only on the cash branch (grep -n "cashRefundOut" matches in Sales.tsx).
    - A decision dialog with both "Haver" and "Dinheiro" options exists and the Haver option is gated on sale.clientId (source assertion).
    - Mixed/split sale: the decision dialog shows the full paidAmount and, when result.otherPaid > 0, a hint line noting the cash/entry portion (source assertion referencing result.otherPaid).
    - Observable E1/E5/I5: estorno of a crediário sale with zero installment.amountPaid produces no storeCredit change and no cashRefundOut, marks sale refunded, flips its open/overdue installments to cancelled, and writes those ids to sale.cancelledInstallmentIds.
    - Observable E3: estorno of a crediário sale with paidAmount > 0 shows the haver/cash dialog; haver adds exactly roundCurrency(paidAmount) to storeCredit; cash adds nothing to storeCredit and sets cashRefundOut; both persist cancelledInstallmentIds.
    - Observable E4: estorno of a FULLY-paid crediário sale (crediario_paid) shows the haver/cash dialog with paidAmount = sum of all installments.amountPaid (+ any non-crediário paymentEntries).
    - Observable I3: partial devolução then estorno restores only the not-yet-returned quantity (no double restock).
    - Observable I1/I6: paid installments (incl. number:0 entrada) stay 'paid'; CreditNotes shows the cancelled ones, not ghosts.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>handleRefund delegates to processRefund, offers haver/cash decision when paid>0 (with mixed-payment hint), persists cancelledInstallmentIds on the refunded sale in all modes, caps haver at the full paid amount, cancels only open/overdue installments, and is double-restock-safe; build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reflect estorno cash-out in fechamento de caixa (distinct from accounting Estornos)</name>
  <files>src/pages/Reports.tsx</files>
  <read_first>
    - src/pages/Reports.tsx lines 26-70 (cashReport useMemo: dayRefunds, totalRefunds) and lines 200-256 (Estornos card + Formas de Pagamento render)
    - src/types/index.ts (Sale.cashRefundOut? added in Plan 01)
    - src/lib/formatters.ts (formatCurrency)
    - .planning/phases/03-estorno-correto/03-CONTEXT.md (EST-03b: cash refund must be distinguishable in fechamento de caixa)
  </read_first>
  <behavior>
    - cashReport gains cashRefundOut = sum of (s.cashRefundOut || 0) across the day's refunded sales (reuse dayRefunds filter that already selects status==='refunded' && day match).
    - The existing "Estornos" total KEEPS its current meaning: the accounting value of reversed sales = sum of sale.total over dayRefunds. Its label and computation are unchanged so prior reports remain comparable.
    - The NEW line shows actual cash disbursed and is clearly labelled "Saída de Caixa (Estorno em Dinheiro)" = sum of cashRefundOut. It is rendered separately from the Estornos card so the two values cannot be confused or double-counted (Estornos = accounting total; Saída de Caixa = real cash that left the register).
    - Backward compatible: refunded sales without cashRefundOut contribute 0; the two figures may legitimately differ (e.g., a zero-paid estorno counts in Estornos with cashRefundOut 0; a haver-mode estorno also has cashRefundOut 0).
  </behavior>
  <action>
    In the cashReport useMemo, after computing dayRefunds/totalRefunds, add `const cashRefundOut = dayRefunds.reduce((sum, s) => sum + (s.cashRefundOut || 0), 0);` and include cashRefundOut in the returned object. Leave the existing totalRefunds (sum of sale.total) and its "Estornos" card label untouched. In the fechamento de caixa render (caixa TabsContent), add a NEW distinct line/card labelled "Saída de Caixa (Estorno em Dinheiro)" formatted with formatCurrency(cashReport.cashRefundOut) — render it within the Formas de Pagamento card as a red negative line (mirroring the existing Taxas de Cartão line styling) OR as its own small card; it MUST be visually separate from the Estornos card and clearly distinguish "valor contábil de estornos" (Estornos) from "dinheiro efetivamente saído do caixa" (Saída de Caixa). Use formatCurrency for display. No new storage key; reads the optional field only.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Reports.tsx cashReport computes cashRefundOut summing s.cashRefundOut over dayRefunds (grep -n "cashRefundOut" matches in both the reduce and the JSX).
    - The existing Estornos total/label (sum of sale.total) is unchanged — it still represents the accounting value of reversed sales (source assertion: totalRefunds computation and its card label untouched).
    - A new, separately-labelled "Saída de Caixa (Estorno em Dinheiro)" figure renders the actual cash disbursed using formatCurrency(cashReport.cashRefundOut), visually distinct from the Estornos card so the two are not double-counted.
    - Sales without cashRefundOut contribute 0 (uses `|| 0` guard — source assertion).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Fechamento de caixa surfaces the cash-out from cash-mode estornos as a clearly labelled, distinct figure (real cash disbursed), separate from the unchanged accounting Estornos total; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → estorno + haver/cash choice | Operator confirms reversal and picks haver or cash; both mutate financial state |
| in-memory state → electron-store | setProducts/setInstallments/setClients/setSales persist the reversal (non-transactional — known tech debt) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | haver-vs-cash branch | mitigate | Haver path increments storeCredit by exactly roundCurrency(paidAmount); cash path never touches storeCredit and records cashRefundOut — the two are mutually exclusive in finalizeRefund (EST-03); paidAmount includes the full paid amount (crediário + non-crediário) so split sales are never under-refunded |
| T-03-05 | Elevation of privilege | haver without client | mitigate | Haver option gated on sale.clientId; no-client sales can only take the cash path (CONTEXT EST-03) |
| T-03-06 | Repudiation | cash-out + cancelled-installment auditability | mitigate | cashRefundOut persisted on the refunded Sale and surfaced distinctly in fechamento de caixa; cancelledInstallmentIds persisted on the refunded Sale in all modes — full audit trail for cash that left the register and for which obligations were cancelled (EST-02, EST-03b) |
| T-03-07 | Tampering | partial reversal on crash | accept | Multi-key writes are non-atomic (documented tech debt OPS2-02, deferred to v2); estorno keeps existing mutation ordering and adds no new partial states |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to audit |
</threat_model>

<verification>
- `npm run build` exits 0 after both tasks.
- handleRefund delegates to processRefund (Plan 01) — grep confirms.
- Haver vs cash are mutually exclusive; haver gated on client; cash recorded as cashRefundOut; paidAmount covers full paid amount for mixed sales.
- cancelledInstallmentIds persisted on the refunded sale in all modes.
- Fechamento de caixa surfaces cash-out as a distinct figure separate from the accounting Estornos total.
- Manual scenarios E1/E3/E4/E5/I1/I3/I5/I6 from FINANCIAL-PITFALLS §5 behave as specified.
</verification>

<success_criteria>
- EST-01: zero-paid crediário estorno generates no haver, only cancels debt.
- EST-02: only open/overdue installments of the sale flip to cancelled; recorded via cancelledInstallmentIds persisted on the refunded Sale; paid untouched.
- EST-03: paid>0 prompts haver vs cash; dialog shows full paidAmount with mixed-payment hint; haver adds roundCurrency(paid) to storeCredit (client required); cash records auditable cash-out, no storeCredit.
- EST-04: estorno after partial devolução does not double-restore stock.
- Reports: cash-out distinct from accounting Estornos total; no double-counting.
- Build green.
</success_criteria>

<output>
Create `.planning/phases/03-estorno-correto/03-02-SUMMARY.md` when done.
</output>
</content>
