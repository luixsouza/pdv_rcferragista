---
phase: 04-devolucao-completa
plan: "04"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/pages/Sales.tsx
autonomous: true
requirements: [DEV-04, DEV-06]
must_haves:
  truths:
    - "A devolução pela tela de Vendas usa o capping de haver (não gera haver por valor não pago)"
    - "Devolução total de venda crediário pela tela de Vendas cancela as parcelas em aberto/vencidas"
    - "O ReturnRecord gravado pela tela de Vendas carrega cancelledInstallmentIds"
    - "A tela de Vendas continua permitindo devolver vendas crediario_pending (canReturn preservado)"
  artifacts:
    - path: "src/pages/Sales.tsx"
      provides: "handleReturnFromSale consome processReturn capping-aware + cancela parcelas no full return; canReturn preservado (permite crediario_pending)"
      contains: "cancelledInstallmentIds"
  key_links:
    - from: "src/pages/Sales.tsx"
      to: "src/lib/processReturn.ts"
      via: "processReturn com installments para capping + conjunto de cancelamento"
      pattern: "processReturn"
    - from: "src/pages/Sales.tsx"
      to: "installments storage"
      via: "setInstallments cancela parcelas em open/overdue no full return"
      pattern: "setInstallments"
---

<objective>
Make the item-return path on the Sales history page (`handleReturnFromSale`) consistent with the upgraded rules core: pass `installments` so haver capping applies (DEV-04, no haver for unpaid crediário), and cancel open/overdue installments on full crediário returns, persisting `cancelledInstallmentIds` on the ReturnRecord (BUG-2 parity). This closes the duplicate-return-logic drift flagged in CONCERNS.md so all three devolução entry points (Sales, Returns, POS) behave identically.

Purpose: Sales.tsx is the historically most-used devolução entry; leaving it on the old uncapped path would reintroduce BUG-1/BUG-2 for that route. DEV-06's correctness (crediario_pending returns) also depends on Sales and Returns behaving the same — and on Sales.tsx `canReturn()` continuing to allow crediario_pending returns (BUG-4 / DEV-06 eligibility must not regress).

Output: `src/pages/Sales.tsx` `handleReturnFromSale` rewired to consume the capping-aware processReturn + installment cancellation, with `canReturn()` eligibility left intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-devolucao-completa/04-CONTEXT.md
@.planning/phases/04-devolucao-completa/04-01-PLAN.md
@.planning/research/FINANCIAL-PITFALLS.md

<interfaces>
<!-- Wave-1 (04-01) upgraded contract. -->

From src/lib/processReturn.ts (after 04-01):
```typescript
// ProcessReturnInput now includes installments: Installment[]
// ProcessReturnResult now includes cancelledInstallmentIds: string[]; creditGenerated capped for crediário
export function processReturn(input: ProcessReturnInput): ProcessReturnResult
```

Existing Sales.tsx facts (do NOT re-architect):
- handleReturnFromSale at lines 309-355 already calls processReturn and persists ReturnRecord + sets sale 'refunded' on allItemsReturned.
- `installments` is loaded via useLocalStorage (used by handleRefund/processRefund in Phase 3) and `setInstallments` available — confirm by grep before editing.
- getReturnedQuantities at lines 276-285 supplies alreadyReturnedQtys.
- canRefund (357-364) already excludes sales with cancelledInstallmentIds set (WR-02 guard from Phase 3) — leave intact.
- canReturn (366-370) gates which sales are returnable from the Sales history; it deliberately allows crediario_pending sales (BUG-4 / DEV-06 eligibility) by NOT excluding that status — it must be left intact. The token `crediario_pending` also appears in canRefund (line 363); DEV-06 eligibility for the return path depends on canReturn not being tightened.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass installments to processReturn and cancel installments on full crediário return in handleReturnFromSale</name>
  <files>src/pages/Sales.tsx</files>
  <read_first>
    - src/pages/Sales.tsx (handleReturnFromSale 309-355, getReturnedQuantities 276-285, useLocalStorage block near top, finalizeRefund/handleRefund 180-240 for the installment-cancellation pattern to mirror, canReturn 366-370 — MUST be left intact)
    - src/lib/processReturn.ts (after 04-01 — installments input, cancelledInstallmentIds result, capped creditGenerated)
    - .planning/research/FINANCIAL-PITFALLS.md §2 (cancel only open/overdue, only on full return, idempotent map pattern)
  </read_first>
  <action>
    Confirm `installments`/`setInstallments` are already loaded in Sales.tsx (Phase 3 introduced processRefund wiring); if not, add the useLocalStorage slice. In handleReturnFromSale (~line 321), add `installments` to the processReturn input object so the capping logic from 04-01 runs. After the existing setProducts/setClients/setReturns calls, when `result.cancelledInstallmentIds.length > 0`, cancel them: `setInstallments(installments.map(inst => result.cancelledInstallmentIds.includes(inst.id) ? { ...inst, status: 'cancelled' } : inst))` — mirroring the handleRefund pattern (map not filter, idempotent). The returnRecord already carries cancelledInstallmentIds from 04-01, so persist it as-is (no manual field set). Update the success toast to report `returnRecord.creditGenerated` (the capped figure) instead of the uncapped item total when a client is present — when creditGenerated is 0 on a crediário sale, the toast should say debt cancelled / no haver generated rather than implying credit was added. Keep the no-client stock-only message.

    DEV-06 regression guard (B-3): do NOT modify `canReturn()` (lines 366-370). It currently allows returning crediario_pending sales (per BUG-4 / DEV-06) by not filtering them out; this eligibility must be preserved exactly so the Sales return path and /returns stay consistent. Only handleReturnFromSale's financial behavior changes in this task — eligibility logic is untouched.

    Reference D-04 / DEV-06 in comments.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - The processReturn call passes installments: `grep -n "installments," src/pages/Sales.tsx` shows it inside the handleReturnFromSale input (distinct from the handleRefund/processRefund call).
    - `grep -n "result.cancelledInstallmentIds.includes" src/pages/Sales.tsx` confirms full-return installment cancellation in the return path.
    - `grep -n "returnRecord.creditGenerated" src/pages/Sales.tsx` confirms the toast uses the capped value.
    - canRefund WR-02 guard intact: `grep -n "cancelledInstallmentIds && sale.cancelledInstallmentIds.length" src/pages/Sales.tsx` still present.
    - B-3 (DEV-06 regression guard): `grep -n "crediario_pending" src/pages/Sales.tsx` confirms canReturn still allows that status (the token remains and canReturn at 366-370 is unchanged — crediario_pending sales stay returnable).
    - No raw uncapped credit in the return toast: the success toast no longer references `returnRecord.totalRefunded` as the "crédito em haver" figure for crediário sales.
  </acceptance_criteria>
  <done>handleReturnFromSale consumes the capping-aware processReturn, cancels open/overdue installments on full crediário returns, persists cancelledInstallmentIds, reports the capped haver, and leaves canReturn intact (crediario_pending sales remain returnable — DEV-06). All three devolução entry points now behave identically. Build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator Sales-history return → financial state | Operator returns items from sale detail; page mutates products, clients, returns, installments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | uncapped haver on Sales return path | mitigate | pass installments → processReturn capping (04-01); toast uses capped creditGenerated |
| T-04-11 | Tampering | ghost installments after full Sales return | mitigate | cancel open/overdue via result.cancelledInstallmentIds when allItemsReturned |
| T-04-12 | Denial of Service | DEV-06 regression — crediario_pending no longer returnable | mitigate | canReturn (366-370) left intact; grep guard confirms crediario_pending still allowed |
| T-04-SC | Tampering | npm installs | mitigate | none — no new packages |
</threat_model>

<verification>
- Returning all items of a zero-paid crediário sale from Sales history generates no haver and cancels its installments.
- Cash-sale returns from Sales history still generate full haver.
- ReturnRecord persisted from Sales carries cancelledInstallmentIds (so /returns reversal can restore them).
- crediario_pending sales remain returnable from Sales history (canReturn intact — DEV-06).
- `npm run build` exits 0.
</verification>

<success_criteria>
- DEV-04: Sales-history return path applies haver capping.
- DEV-06 consistency: Sales and Returns devolução behave identically for crediário (incl. crediario_pending), including installment cancellation on full return; canReturn eligibility preserved.
</success_criteria>

<output>
Create `.planning/phases/04-devolucao-completa/04-04-SUMMARY.md` when done.
</output>
</content>
