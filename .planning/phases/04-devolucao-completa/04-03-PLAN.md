---
phase: 04-devolucao-completa
plan: "03"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/pages/POS.tsx
autonomous: true
requirements: [DEV-01, DEV-02, DEV-03, DEV-04]
must_haves:
  truths:
    - "O operador inicia uma devolução diretamente pela tela do PDV (sem ir a Vendas/Devoluções)"
    - "É possível localizar a venda por código ou cliente dentro do PDV"
    - "É possível devolver itens de uma venda feita sem cliente (estoque restaurado)"
    - "Durante a devolução o operador pode associar um cliente existente ou cadastrar um novo no ato"
    - "Quando há cliente, o haver gerado respeita o capping (consome processReturn de 04-01)"
  artifacts:
    - path: "src/pages/POS.tsx"
      provides: "Modo/diálogo de devolução no PDV: busca de venda, checklist de itens, associação/cadastro de cliente, confirmação via processReturn"
      contains: "processReturn"
  key_links:
    - from: "src/pages/POS.tsx"
      to: "src/lib/processReturn.ts"
      via: "processReturn para calcular devolução e capping de haver"
      pattern: "processReturn"
    - from: "src/pages/POS.tsx"
      to: "returns storage"
      via: "registrar ReturnRecord (setReturns)"
      pattern: "setReturns"
    - from: "src/pages/POS.tsx"
      to: "src/components/ClientCombobox.tsx"
      via: "associar cliente existente durante a devolução"
      pattern: "ClientCombobox"
---

<objective>
Add a devolução entry point inside the POS screen (DEV-01): a "Devolução" mode/dialog that finds a sale by code or client, shows the returnable items checklist, and confirms via the Wave-1 `processReturn` core. Support returning sales that have no client (DEV-02, stock-only) and let the operator associate an existing client or register a new one during the return (DEV-03) to enable capped haver (DEV-04).

Purpose: Today devolução is only reachable from Sales history or the /returns page — the operator at the register must navigate away. This makes the most common counter operation (customer returns goods at the till) one click from the POS.

Output: `src/pages/POS.tsx` gains a return mode reusing ClientCombobox + the existing client-creation shape, delegating all financial mutation to processReturn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-devolucao-completa/04-CONTEXT.md
@.planning/phases/04-devolucao-completa/04-01-PLAN.md
@src/pages/POS.tsx
@src/components/ClientCombobox.tsx

<interfaces>
<!-- Wave-1 (04-01) and reusable shapes. -->

From src/lib/processReturn.ts (after 04-01):
```typescript
// pass installments so haver capping applies; result.creditGenerated is capped, cancelledInstallmentIds returned
export function processReturn(input: ProcessReturnInput): ProcessReturnResult
```

Existing POS.tsx facts the executor must reuse (do NOT re-architect the page):
- useLocalStorage slices already present: products, clients, sales, installments (lines 59-62). MUST add `returns` slice: `const [returns, setReturns] = useLocalStorage<ReturnRecord[]>('returns', [])`.
- ClientCombobox already imported (line 16) and used for sale client selection.
- toast from sonner; formatCurrency/roundCurrency from @/lib/formatters.
- Sale search analog exists in Returns.tsx (search by id.toUpperCase().includes(code) OR clientName); replicate in POS for the return picker.

Client-creation shape to reuse (from Clients.tsx handleSave lines 100-106):
```typescript
const newClient: Client = { ...formData, id: crypto.randomUUID(), createdAt: now, updatedAt: now };
setClients([...clients, newClient]);
// formData minimal required: name (required), document/email/phone/address/city default ''
```

getReturnedQuantities analog (Returns.tsx 86-95 / Sales.tsx 276-285): sum non-reversed ReturnRecord item quantities per productId for a sale — needed for processReturn.alreadyReturnedQtys and to compute maxReturnable per item.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add return mode state, returns storage, and sale-finding UI in POS</name>
  <files>src/pages/POS.tsx</files>
  <read_first>
    - src/pages/POS.tsx (imports 1-38, useLocalStorage 59-62, state block 64-101, ClientCombobox usage)
    - src/pages/Returns.tsx (eligibleSales 60-62, searchedSales 70-75, getReturnedQuantities 86-95, selectSale 97-117 — UI patterns to mirror)
    - src/types/index.ts (ReturnRecord, Sale, SaleItem)
  </read_first>
  <action>
    Add a `returns` slice: `const [returns, setReturns] = useLocalStorage<ReturnRecord[]>('returns', [])`. Import ReturnRecord and processReturn. Add return-mode state: `returnDialogOpen` (boolean), `returnSaleSearch` (string), `returnSelectedSale` (Sale | null), `returnItems` ({ item: SaleItem; quantity: number; selected: boolean }[]), `returnClientId` (string), and a `RotateCcw` icon import from lucide-react. Add a getReturnedQuantities helper mirroring Returns.tsx (filter non-reversed returns for the sale, sum per productId). Add a "Devolução" trigger button in the POS header/toolbar area (Claude's discretion on exact placement — a Button near the cart/client controls) that opens a Dialog (use shadcn Dialog; import if not present). Inside the dialog, step 1: a search Input (filter eligibleSales — sales where status is 'completed' | 'crediario_paid' | 'crediario_pending' and not 'refunded' — by id.toUpperCase().includes(search.toUpperCase()) OR clientName match, slice(0,10)); clicking a result sets returnSelectedSale, pre-fills returnClientId from sale.clientId (may be empty), and builds returnItems from maxReturnable (item.quantity - alreadyReturned, filtered > 0). Do NOT wire confirmation yet (Task 2). Build must pass.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -n "useLocalStorage<ReturnRecord\[\]>('returns'" src/pages/POS.tsx` confirms returns slice.
    - `grep -n "returnDialogOpen\|returnSelectedSale\|returnItems" src/pages/POS.tsx` confirms return-mode state.
    - `grep -n "getReturnedQuantities" src/pages/POS.tsx` confirms the helper exists.
    - `grep -n "Devolu" src/pages/POS.tsx` confirms a return trigger label is present in the UI.
    - `grep -n "crediario_pending" src/pages/POS.tsx` confirms the return picker eligibility includes pending crediário (DEV consistency).
  </acceptance_criteria>
  <done>POS has a Devolução trigger opening a dialog that finds a sale (by code/client incl. crediario_pending) and lists returnable items. Build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Item checklist, client association/inline registration (DEV-02/03), and confirm via processReturn (DEV-01/04)</name>
  <files>src/pages/POS.tsx</files>
  <read_first>
    - src/pages/POS.tsx (return-mode UI from Task 1)
    - src/pages/Returns.tsx (step-3 checklist 419-512, handleReturn haver branch after 04-02 if available — but POS only needs the haver/stock path; abatimento is /returns-only per CONTEXT)
    - src/pages/Clients.tsx (handleSave 76-111 — client-creation shape to reuse for inline registration)
    - src/lib/processReturn.ts (after 04-01 — processReturn input/result)
    - src/components/ClientCombobox.tsx (props: clients, value, onChange)
  </read_first>
  <action>
    In the dialog step 2 (after a sale is selected): render returnItems as a checklist (Checkbox + qty Input + line total via formatCurrency), mirroring Returns.tsx. Show a client section: a ClientCombobox bound to returnClientId for associating an existing client, plus a small "Cadastrar cliente" affordance that reveals minimal fields (name required, document/phone optional) and on save creates a Client using the Clients.tsx shape (`{ ...minimalFields, id: crypto.randomUUID(), createdAt, updatedAt }`), appends via setClients, and sets returnClientId to the new id (DEV-03). When the sale/return has no client and the operator leaves returnClientId empty, the return proceeds stock-only (DEV-02) — show an amber note that no haver will be generated. Add a confirm handler that calls processReturn({ sale: returnSelectedSale, itemsToReturn (mapped from selected returnItems), products, clients, installments, clientId: returnClientId || undefined, clientName: resolved name, alreadyReturnedQtys: getReturnedQuantities(sale.id) }); then setProducts(updatedProducts), setClients(updatedClients), setReturns([...returns, returnRecord]); when result.cancelledInstallmentIds.length > 0 apply setInstallments(installments.map(i => result.cancelledInstallmentIds.includes(i.id) ? { ...i, status: 'cancelled' } : i)); set sale status to 'refunded' when allItemsReturned. Toast uses returnRecord.creditGenerated (capped) when client present, else the stock-only message. Reset return state + close dialog after confirm. POS only offers the haver/stock modality (abatimento lives on /returns per CONTEXT — do not add abatimento here). Reference D-01/D-02/D-03/D-04 in comments.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -n "processReturn(" src/pages/POS.tsx` confirms the return is computed via the shared core.
    - `grep -n "setReturns" src/pages/POS.tsx` confirms ReturnRecord persistence.
    - `grep -n "crypto.randomUUID" src/pages/POS.tsx` confirms inline client creation path exists (in addition to any sale id generation — verify the new client-creation block).
    - `grep -n "ClientCombobox" src/pages/POS.tsx` confirms client association in the return dialog (in addition to the sale flow).
    - `grep -n "status: 'cancelled'" src/pages/POS.tsx` confirms full-crediário-return cancels installments (consistent with /returns).
    - `grep -n "creditGenerated" src/pages/POS.tsx` confirms the toast/credit uses the capped value, not raw item totals.
    - `grep -c "processAbatement" src/pages/POS.tsx` returns 0 (abatimento intentionally not in POS).
  </acceptance_criteria>
  <done>POS confirms a return via processReturn (capped haver, installment cancellation on full crediário return, stock restored), supports no-client returns, and lets the operator associate or register a client inline. Build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator POS return dialog → sales/stock/client state | Operator finds sale + items + optional client; page mutates products, returns, installments, clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | haver from POS return | mitigate | delegate to processReturn (04-01 capping); never compute creditGenerated from raw item totals in POS |
| T-04-08 | Tampering | double return of same items | mitigate | reuse getReturnedQuantities → alreadyReturnedQtys; maxReturnable clamps qty |
| T-04-09 | Elevation | inline client creation bypassing validation | accept | minimal-field creation mirrors existing Clients.tsx flow (name required); document validation is best-effort, consistent with current app |
| T-04-SC | Tampering | npm installs | mitigate | none — no new packages |
</threat_model>

<verification>
- A return can be started and completed entirely within the POS screen.
- No-client sale return restores stock; operator can add a client to generate (capped) haver.
- Inline-registered client receives the haver.
- Full crediário return from POS cancels open/overdue installments.
- `npm run build` exits 0.
</verification>

<success_criteria>
- DEV-01: devolução initiable from POS.
- DEV-02: no-client sale returnable (stock-only).
- DEV-03: client can be associated or registered during the return.
- DEV-04: haver generated via processReturn capping (no over-credit).
</success_criteria>

<output>
Create `.planning/phases/04-devolucao-completa/04-03-SUMMARY.md` when done.
</output>
