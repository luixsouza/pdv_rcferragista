---
phase: 04-devolucao-completa
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/pages/Returns.tsx
  - src/types/index.ts
autonomous: true
requirements: [DEV-04, DEV-05, DEV-06, DEV-07]
must_haves:
  truths:
    - "A página de Devoluções lista e devolve vendas com status crediario_pending"
    - "O operador escolhe a modalidade: gerar haver OU abatimento de débito"
    - "Na modalidade abatimento o operador seleciona a(s) parcela(s) e nenhum haver é gerado"
    - "Devolução total de venda crediário cancela as parcelas em aberto/vencidas e grava cancelledInstallmentIds"
    - "Reverter uma devolução restaura as parcelas canceladas (open/overdue) e estorna o abatimento"
  artifacts:
    - path: "src/pages/Returns.tsx"
      provides: "Seleção de modalidade, eligibleSales com crediario_pending, cancelamento de parcelas, reversão consistente"
      contains: "processAbatement"
    - path: "src/types/index.ts"
      provides: "ReturnRecord.abatedInstallments? para reversão de abatimento (retrocompatível)"
      contains: "abatedInstallments"
  key_links:
    - from: "src/pages/Returns.tsx"
      to: "src/lib/processReturn.ts"
      via: "processReturn (haver capping) e processAbatement (modalidade B)"
      pattern: "processReturn|processAbatement"
    - from: "src/pages/Returns.tsx"
      to: "installments storage"
      via: "setInstallments para cancelar/restaurar/abater parcelas"
      pattern: "setInstallments"
    - from: "src/pages/Returns.tsx"
      to: "creditPayments storage"
      via: "registro auditável de abatimento"
      pattern: "creditPayments|setCreditPayments"
---

<objective>
Upgrade the dedicated Devoluções page to consume the Wave-1 rules core: include `crediario_pending` sales (DEV-06, corrects BUG-4), add a modality choice (haver vs abatimento de débito) where abatimento lets the operator pick installments (DEV-05) and generates no haver, cancel open/overdue installments on full crediário returns (BUG-2) recording `cancelledInstallmentIds`, and make `handleReverseReturn` restore those cancelled installments and reverse abatimento CreditPayments (DEV-07).

Purpose: The /returns page is the discoverable devolução workflow; today it can't touch the most common crediário case and silently mints haver. This plan makes it financially correct and feature-complete for crediário.

Output: A reworked `src/pages/Returns.tsx` with modality UI, crediario_pending eligibility, installment cancellation, and consistent reversal, plus a retrocompatible `ReturnRecord.abatedInstallments?` field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-devolucao-completa/04-CONTEXT.md
@.planning/phases/04-devolucao-completa/04-01-PLAN.md
@.planning/research/FINANCIAL-PITFALLS.md
@src/pages/Returns.tsx

<interfaces>
<!-- Wave-1 (04-01) upgraded contracts the executor consumes. -->

From src/lib/processReturn.ts (after 04-01):
```typescript
// ProcessReturnInput now includes installments: Installment[]
// ProcessReturnResult now includes cancelledInstallmentIds: string[]; creditGenerated is capped for crediário
export function processReturn(input: ProcessReturnInput): ProcessReturnResult

export interface ProcessAbatementInput { sale: Sale; returnValue: number; selectedInstallmentIds: string[]; installments: Installment[]; clientId: string; clientName: string; }
export interface ProcessAbatementResult { updatedInstallments: Installment[]; creditPayments: CreditPayment[]; abatedTotal: number; residual: number; abatedMap: { installmentId: string; amount: number }[]; }
export function processAbatement(input: ProcessAbatementInput): ProcessAbatementResult
```

From src/lib/installmentStatus.ts:
```typescript
export const getEffectiveStatus: (inst: Installment, now?: Date) => 'open'|'paid'|'overdue'|'cancelled';
```

Existing Returns.tsx structure (lines to modify): eligibleSales (60-62), handleReturn (123-183), handleReverseReturn (185-236), step-3 summary card (419-512). Returns.tsx already imports ClientCombobox, Tabs, AlertDialog, Checkbox; uses useLocalStorage for sales/clients/products/returns; has getReturnedQuantities (86-95).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Include crediario_pending sales (DEV-06) and add installments + creditPayments storage with modality state</name>
  <files>src/pages/Returns.tsx</files>
  <read_first>
    - src/pages/Returns.tsx (eligibleSales line 60-62, useLocalStorage block 33-36, handleReturn 123-183, step-3 card 419-512)
    - src/pages/Sales.tsx (canReturn line 366-370 — the alignment target for eligibility)
    - src/lib/processReturn.ts (after 04-01 — ProcessReturnResult.cancelledInstallmentIds, processAbatement)
    - src/types/index.ts (Installment, CreditPayment, Sale.status enum)
  </read_first>
  <action>
    Align eligibleSales (Returns.tsx ~line 60) to Sales.tsx canReturn: include `crediario_pending` alongside `completed` and `crediario_paid`, still excluding `refunded` (DEV-06, corrects BUG-4). Add two useLocalStorage slices: `const [installments, setInstallments] = useLocalStorage<Installment[]>('installments', [])` and a credit-payments slice — confirm the existing storage key name used in CreditNotes.tsx by grepping for `useLocalStorage<CreditPayment` and reuse that exact key. Import processAbatement, Installment, CreditPayment, and getEffectiveStatus. Add modality state: `const [returnModality, setReturnModality] = useState<'haver' | 'abatimento'>('haver')` and `const [selectedInstallmentIds, setSelectedInstallmentIds] = useState<string[]>([])`. Derive the sale's open/overdue installments for the abatimento picker: `saleOpenInstallments = installments.filter(i => i.saleId === selectedSale?.id && (getEffectiveStatus(i) === 'open' || getEffectiveStatus(i) === 'overdue'))`. Pass `installments` into the existing processReturn call so capping works. Do NOT yet wire the abatement branch (Task 2). Keep all existing JSX working; build must pass.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -n "crediario_pending" src/pages/Returns.tsx` confirms eligibleSales now includes it.
    - `grep -n "useLocalStorage<Installment\[\]>('installments'" src/pages/Returns.tsx` and `grep -n "CreditPayment" src/pages/Returns.tsx` confirm both slices loaded.
    - `grep -n "returnModality\|selectedInstallmentIds" src/pages/Returns.tsx` confirms modality state.
    - processReturn call now passes installments: `grep -n "installments," src/pages/Returns.tsx` shows it inside the processReturn input.
  </acceptance_criteria>
  <done>Returns page treats crediario_pending as eligible, loads installments + creditPayments, and holds modality/selected-installment state. Build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire modality UI (haver vs abatimento) + installment cancellation in handleReturn (DEV-04/05, BUG-2)</name>
  <files>src/pages/Returns.tsx, src/types/index.ts</files>
  <read_first>
    - src/pages/Returns.tsx (handleReturn 123-183, step-3 summary card 419-512 after Task 1)
    - src/lib/processReturn.ts (processReturn cancelledInstallmentIds, processAbatement signature/result)
    - .planning/research/FINANCIAL-PITFALLS.md §2 (cancel only open/overdue on full return) and §3 (abatement, residual handling) and A1–A4 edge cases
    - src/types/index.ts (ReturnRecord — to add abatedInstallments)
    - src/components/ui/alert-dialog usage already in this file; Checkbox usage in step-3 card
  </read_first>
  <action>
    First add an optional field to ReturnRecord in src/types/index.ts: `abatedInstallments?: { installmentId: string; amount: number }[]` (retrocompatible, mirrors cancelledInstallmentIds; consumed by Task 3 reversal).

    In the step-3 card, add a modality selector (two options — "Gerar Haver" and "Abatimento de Débito") shown only when the sale is crediário and has a client; for non-crediário or no-client sales force 'haver' and hide the abatimento option. When 'abatimento' is chosen, render the saleOpenInstallments as a checklist (Checkbox per installment showing number, dueDate, remaining = amount - amountPaid - discountApplied via formatCurrency) bound to selectedInstallmentIds. Show a live computed "a abater R$ X / resíduo R$ Y" summary by calling processAbatement in a useMemo against the current totalRefund and selectedInstallmentIds (display only).

    Rewrite handleReturn to branch on returnModality:
    - 'haver' branch: call processReturn (now capping-aware) exactly as today; additionally, when result.cancelledInstallmentIds.length > 0, apply `setInstallments(installments.map(inst => result.cancelledInstallmentIds.includes(inst.id) ? { ...inst, status: 'cancelled' } : inst))` (BUG-2); the returnRecord already carries cancelledInstallmentIds from 04-01 so persist it as-is in setReturns. Toast reflects capped creditGenerated (use returnRecord.creditGenerated, not totalRefund).
    - 'abatimento' branch: require selectedInstallmentIds non-empty (toast.error otherwise); call processReturn to build the stock-restoring updatedProducts + returnRecord, but force the persisted returnRecord.creditGenerated = 0 and do NOT increment storeCredit (use updatedProducts only, keep clients unchanged); then call processAbatement({ sale, returnValue: returnRecord.totalRefunded, selectedInstallmentIds, installments, clientId, clientName }); setInstallments(abResult.updatedInstallments); setCreditPayments([...creditPayments, ...abResult.creditPayments]); persist abResult.abatedMap onto the returnRecord as `returnRecord.abatedInstallments = abResult.abatedMap`. If abResult.residual > 0, toast.warning telling the operator the excess (formatCurrency(residual)) was NOT applied and they may select more installments — never convert to haver (CONTEXT discretion). On full crediário return in abatimento mode, still set sale status to refunded if allItemsReturned.

    Reset modality + selectedInstallmentIds in the existing reset paths. Reference D-04/D-05 in comments on each branch.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -n "abatedInstallments" src/types/index.ts` confirms the optional field added.
    - `grep -n "processAbatement" src/pages/Returns.tsx` confirms abatement is invoked in handleReturn.
    - `grep -n "setInstallments" src/pages/Returns.tsx` confirms installment cancellation (haver branch) and abatement update (abatimento branch).
    - `grep -n "status: 'cancelled'" src/pages/Returns.tsx` confirms full-return cancellation (BUG-2 fix).
    - `grep -n "residual" src/pages/Returns.tsx` confirms the excess warning path (no silent haver).
    - `grep -n "creditGenerated" src/pages/Returns.tsx` confirms abatimento forces 0 / haver uses capped value (toast no longer references raw totalRefund for the credit figure).
    - `grep -n "setCreditPayments" src/pages/Returns.tsx` confirms abatement audit records persisted.
  </acceptance_criteria>
  <done>The Returns page offers haver vs abatimento; haver applies capped credit and cancels open/overdue installments on full returns; abatimento reduces chosen installments, generates no haver, warns on residual, and records auditable CreditPayments + abatedInstallments. Build is green.</done>
</task>

<task type="auto">
  <name>Task 3: Restore cancelled installments and reverse abatimento on handleReverseReturn (DEV-07)</name>
  <files>src/pages/Returns.tsx</files>
  <read_first>
    - src/pages/Returns.tsx (handleReverseReturn 185-236 after Task 2)
    - .planning/research/FINANCIAL-PITFALLS.md §2 "Restore installments" (restore to 'open', overdue re-evaluated) and §3 "Abatement Reversal" (decrement discountApplied via abatedMap, Math.max(0,...) guard)
    - src/types/index.ts (ReturnRecord.abatedInstallments added in Task 2)
  </read_first>
  <action>
    In handleReverseReturn, after marking reversedAt:
    - If `ret.cancelledInstallmentIds?.length`, restore them: `setInstallments(installments.map(inst => ret.cancelledInstallmentIds!.includes(inst.id) ? { ...inst, status: 'open', paidAt: undefined } : inst))` — restore to 'open'; overdue detection re-evaluates on next mount via getEffectiveStatus (FINANCIAL-PITFALLS §2). Guard with `&&` length check so legacy records without the field don't crash.
    - If `ret.abatedInstallments?.length` (abatimento modality, identified by creditGenerated === 0 + presence of the map), reverse each: for each { installmentId, amount }, set the installment's discountApplied = Math.max(0, (discountApplied || 0) - amount) and, if it was flipped to 'paid' by the abatement and now no longer fully covered (amountPaid + discountApplied < amount), restore status to 'open'. Also remove the matching 'abatimento' CreditPayments created by this return — filter creditPayments to drop entries whose installmentId is in this return's abatedInstallments and type === 'abatimento' for this sale. Use a single setInstallments and a single setCreditPayments call.
    - Only deduct storeCredit for haver-modality returns (keep the existing `ret.creditGenerated > 0` guard so abatimento reversal doesn't touch storeCredit).
    Reference D-07 in comments. Update the reversal AlertDialog description to mention parcels restored when applicable.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -n "cancelledInstallmentIds" src/pages/Returns.tsx` confirms reversal reads it to restore installments.
    - `grep -n "status: 'open'" src/pages/Returns.tsx` confirms restoration to open (not overdue).
    - `grep -n "Math.max(0" src/pages/Returns.tsx` confirms the discountApplied guard on abatement reversal.
    - `grep -n "abatedInstallments" src/pages/Returns.tsx` confirms abatement reversal path.
    - The `ret.creditGenerated > 0` storeCredit guard remains: `grep -n "creditGenerated > 0" src/pages/Returns.tsx` finds it on the storeCredit deduction.
  </acceptance_criteria>
  <done>Reversing a devolução restores cancelled installments to open, reverses abatimento discountApplied (guarded, removes audit CreditPayments), and only deducts storeCredit for haver-modality returns. Build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator return UI → crediário state | Operator picks modality/installments; page mutates installments, creditPayments, clients, returns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | full-return ghost installments | mitigate | cancel only open/overdue (via 04-01 cancelledInstallmentIds), only when allItemsReturned |
| T-04-05 | Elevation | abatimento without client/crediário | mitigate | abatimento option hidden unless crediário + clientId; selectedInstallmentIds required |
| T-04-06 | Repudiation | reversal leaves inconsistent crediário | mitigate | restore cancelledInstallmentIds to open; reverse abatedInstallments discountApplied with Math.max(0,...); remove abatement CreditPayments |
| T-04-SC | Tampering | npm installs | mitigate | none — no new packages |
</threat_model>

<verification>
- crediario_pending sale is selectable and returnable on /returns.
- Zero-paid crediário full return → creditGenerated 0 + installments cancelled.
- Abatimento reduces chosen installment, generates no haver, residual warned not converted.
- Reverse-return restores cancelled installments to open and reverses abatimento.
- `npm run build` exits 0.
</verification>

<success_criteria>
- DEV-06: /returns processes crediario_pending sales like paid sales.
- DEV-04: haver on /returns is capped (consumes 04-01).
- DEV-05: abatimento modality functional with operator-chosen installments, no haver, explicit residual.
- DEV-07: reversal restores installments + reverses abatimento, leaving crediário consistent.
</success_criteria>

<output>
Create `.planning/phases/04-devolucao-completa/04-02-SUMMARY.md` when done.
</output>
