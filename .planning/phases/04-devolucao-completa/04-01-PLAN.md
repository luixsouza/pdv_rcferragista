---
phase: 04-devolucao-completa
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/processReturn.ts
  - src/types/index.ts
autonomous: true
requirements: [DEV-04, DEV-05]
must_haves:
  truths:
    - "Devolução de venda crediário com zero pago gera haver = R$ 0 (não o valor de varejo)"
    - "Devolução de venda à vista (dinheiro/cartão/pix) continua gerando haver = valor devolvido"
    - "Devolução total de venda crediário retorna a lista de parcelas em aberto/vencidas a cancelar"
    - "A modalidade abatimento abate parcela(s) escolhida(s) sem gerar haver (creditGenerated = 0)"
    - "O excedente do abatimento nunca vira haver — é retornado ao chamador como resíduo explícito"
    - "A superfície de tipos da devolução (CreditPayment.type 'abatimento' + ReturnRecord.abatedInstallments?) fica completamente definida nesta Wave 1, pronta para os consumidores da Wave 2"
  artifacts:
    - path: "src/lib/processReturn.ts"
      provides: "Capping de haver para crediário, conjunto cancelledInstallmentIds, e função pura processAbatement"
      contains: "processAbatement"
    - path: "src/types/index.ts"
      provides: "CreditPayment.type aceita 'abatimento' e ReturnRecord.abatedInstallments? (opcionais, retrocompatíveis) — superfície de tipos completa para a Wave 2"
      contains: "abatimento"
    - path: "src/types/index.ts"
      provides: "ReturnRecord.abatedInstallments? para reversão de abatimento na Wave 2 (DEV-07), apenas consumido em runtime pelos planos da Wave 2"
      contains: "abatedInstallments"
  key_links:
    - from: "src/lib/processReturn.ts"
      to: "src/lib/installmentStatus.ts"
      via: "import getEffectiveStatus para classificar open/overdue antes de cancelar"
      pattern: "getEffectiveStatus"
    - from: "src/lib/processReturn.ts"
      to: "src/lib/formatters.ts"
      via: "roundCurrency em cada cálculo monetário"
      pattern: "roundCurrency"
---

<objective>
Upgrade the pure `processReturn.ts` module to be the single source of truth for the financial rules of devolução: haver capping for crediário sales (DEV-04, corrects BUG-1), the cancelled-installment set on full returns (foundation for BUG-2 fix consumed by the UI plans), and a new `processAbatement` function for the "abatimento de débito" modality (DEV-05). Extend `CreditPayment.type` to record an auditable abatimento entry AND add `ReturnRecord.abatedInstallments?` so the ENTIRE devolução type surface is defined here in Wave 1 — Wave 2 plans only consume these fields at runtime, never redefine them.

Purpose: Centralizing these rules here (mirroring the Phase 3 `processRefund.ts` pattern) means Returns.tsx, POS.tsx and Sales.tsx all get correct, consistent financial behavior in Wave 2 without re-implementing or drifting (eliminates the duplicate-return-logic concern from CONCERNS.md). Defining all new type fields in this single plan (the only Wave-1 owner of `src/types/index.ts`) removes the shared-file write conflict between parallel Wave-2 plans (B-1/B-2): no Wave-2 plan edits `src/types/index.ts`.

Output: An upgraded `src/lib/processReturn.ts` (haver capping + cancelledInstallmentIds + processAbatement), a retrocompatible `CreditPayment.type` extension, and a retrocompatible `ReturnRecord.abatedInstallments?` field. All four Wave-2 consumers depend on this plan and on no other for the type surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-devolucao-completa/04-CONTEXT.md
@.planning/research/FINANCIAL-PITFALLS.md

<interfaces>
<!-- Current processReturn.ts contract (Phase 1) — Wave 2 callers rely on these and they MUST remain backward-compatible. -->

From src/lib/processReturn.ts (current):
```typescript
export interface ReturnItemInput { productId: string; productName: string; quantity: number; unitPrice: number; costPrice?: number; }
export interface ProcessReturnInput { sale: Sale; itemsToReturn: ReturnItemInput[]; products: Product[]; clients: Client[]; clientId: string | undefined; clientName: string; alreadyReturnedQtys: Record<string, number>; }
export interface ProcessReturnResult { returnRecord: ReturnRecord; updatedProducts: Product[]; updatedClients: Client[]; allItemsReturned: boolean; }
export function processReturn(input: ProcessReturnInput): ProcessReturnResult
```

From src/lib/installmentStatus.ts (Phase 2):
```typescript
export const getEffectiveStatus: (inst: Installment, now?: Date) => 'open' | 'paid' | 'overdue' | 'cancelled';
export const isInstallmentOverdue: (inst: Installment, now?: Date) => boolean;
```

From src/lib/formatters.ts:
```typescript
export const roundCurrency: (value: number) => number; // Math.round(value*100)/100
```

From src/types/index.ts (BEFORE this plan):
```typescript
export interface Installment { id; saleId; clientId; clientName; number; totalInstallments; amount; amountPaid; discountApplied?; dueDate; status: 'open'|'paid'|'overdue'|'cancelled'; paidAt?; ... }
export interface CreditPayment { id; saleId; installmentId?; clientId; clientName; amount; paymentMethod: 'cash'|'credit'|'debit'|'pix'; type?: 'payment'|'discount'; interestAmount?; createdAt; }
export interface ReturnRecord { id; originalSaleId; clientId; clientName; items: SaleItem[]; totalRefunded; creditGenerated; createdAt; reversedAt?; cancelledInstallmentIds?: string[]; }
```

From src/types/index.ts (AFTER this plan — the complete Wave-2 surface; Wave 2 only reads these, never edits the file):
```typescript
export interface CreditPayment { ...; type?: 'payment'|'discount'|'abatimento'; ... }   // 'abatimento' added here
export interface ReturnRecord { ...; cancelledInstallmentIds?: string[]; abatedInstallments?: { installmentId: string; amount: number }[]; }   // abatedInstallments added here
```

How Phase 3 processRefund solved haver capping (reference pattern, do NOT duplicate — devolução is item-scoped, refund is whole-sale):
- crediarioPaid derived from `sum(installments where saleId === sale.id, i => i.amountPaid)` — ground truth, never `sale.crediarioPaid`.
- cancelled set = installments for this sale whose `getEffectiveStatus(i)` is 'open' or 'overdue' (excludes 'paid' to preserve history, 'cancelled' for idempotency).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 'abatimento' to CreditPayment.type, add ReturnRecord.abatedInstallments?, and upgrade processReturn with haver capping + cancelled-installment set</name>
  <files>src/types/index.ts, src/lib/processReturn.ts</files>
  <read_first>
    - src/types/index.ts (CreditPayment line 75-87, Installment line 89-104, ReturnRecord line 106-117, Sale line 49-73)
    - src/lib/processReturn.ts (entire current module — the contract to extend)
    - src/lib/installmentStatus.ts (getEffectiveStatus — analog for open/overdue classification)
    - src/lib/processRefund.ts (Phase 3 pattern: how crediarioPaid sum + getEffectiveStatus cancelled-set were done; mirror the approach)
    - .planning/research/FINANCIAL-PITFALLS.md §1 (haver capping algorithm + edge case table) and §2 (installment cancellation rule, only open/overdue)
  </read_first>
  <behavior>
    - Cash/card/pix sale (no crediario method), full or partial return → creditGenerated = totalRefunded (preserve current correct behavior).
    - Crediário sale, zero installments paid, any return → creditGenerated = 0 (BUG-1 fix; haver capping per DEV-04).
    - Crediário sale, partial payment (e.g. R$50 of R$200 paid), full return → creditGenerated = min(totalRefunded, roundCurrency(totalRefunded * paidProportion)) where paidProportion = sum(installment.amountPaid for this sale) / sale.total.
    - No client (clientId falsy) → creditGenerated = 0 regardless of payment method (stock-only return).
    - Full return of a crediário sale (allItemsReturned === true) → result.cancelledInstallmentIds = ids of this sale's installments whose getEffectiveStatus is 'open' or 'overdue'; partial return → cancelledInstallmentIds = [] (client still owes remainder).
    - Already-paid installments (status 'paid') are NEVER in cancelledInstallmentIds.
    - The TypeScript surface compiles with both new optional fields present: a CreditPayment may carry type 'abatimento' and a ReturnRecord may carry abatedInstallments without breaking existing records (both optional/retrocompatible).
  </behavior>
  <action>
    In src/types/index.ts, make TWO additive, retrocompatible type changes so the entire devolução type surface is owned by this Wave-1 plan and NO Wave-2 plan needs to touch this file:
    1. Widen CreditPayment.type from `'payment' | 'discount'` to `'payment' | 'discount' | 'abatimento'` (DEV-05 audit type; the field stays optional).
    2. Add to ReturnRecord an optional field `abatedInstallments?: { installmentId: string; amount: number }[]` immediately after the existing `cancelledInstallmentIds?: string[]` line (DEV-07 reversal key; mirrors the cancelledInstallmentIds precedent, optional and retrocompatible with electron-store). Wave-2 plan 04-02 only SETS `returnRecord.abatedInstallments` at runtime — it does not redefine the field.

    In src/lib/processReturn.ts: add `installments: Installment[]` to ProcessReturnInput (additive — Wave 2 callers will pass it; document that omitting it via the existing callers is handled by treating undefined as []). Import getEffectiveStatus from @/lib/installmentStatus and Installment from @/types. Replace the `creditGenerated = hasClient ? totalRefunded : 0` rule with the capping algorithm from FINANCIAL-PITFALLS §1: detect isCrediarioSale via `sale.paymentMethod === 'crediario' || sale.paymentEntries?.some(e => e.method === 'crediario')`; if not crediário (or no client) keep current behavior (hasClient ? totalRefunded : 0); if crediário, compute crediarioPaidSoFar = sum of `i.amountPaid` over installments where `i.saleId === sale.id`, paidProportion = sale.total > 0 ? crediarioPaidSoFar / sale.total : 0, and creditGenerated = roundCurrency(Math.min(totalRefunded, totalRefunded * paidProportion)). Add to ProcessReturnResult a `cancelledInstallmentIds: string[]` field: when allItemsReturned && isCrediarioSale, set it to installments for this sale where getEffectiveStatus(i) is 'open' or 'overdue', mapped to ids; otherwise []. Populate `returnRecord.cancelledInstallmentIds` from the same set. Keep updatedClients incrementing storeCredit by creditGenerated (NOT totalRefunded) so the client-credit mutation matches the capped haver. Preserve all stock-restock logic (mil /1000) and allItemsReturned computation unchanged. Do NOT place fenced code in this file beyond normal source. Reference D-04 (DEV-04) in a code comment on the capping branch.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -n "paidProportion" src/lib/processReturn.ts` finds the capping computation.
    - `grep -n "getEffectiveStatus" src/lib/processReturn.ts` confirms open/overdue classification is used for cancelledInstallmentIds.
    - `grep -n "cancelledInstallmentIds" src/lib/processReturn.ts` confirms the field is set on both ProcessReturnResult and returnRecord.
    - `grep -c "hasClient ? totalRefunded : 0" src/lib/processReturn.ts` returns 0 for the crediário branch (the unconditional rule is replaced; the cash branch may still use the expression).
    - `grep -n "abatimento" src/types/index.ts` confirms the CreditPayment.type widening.
    - `grep -n "abatedInstallments" src/types/index.ts` confirms ReturnRecord now carries the optional abatedInstallments field (the full Wave-2 type surface is defined here — B-1/B-2).
    - storeCredit increment uses `creditGenerated`, not `totalRefunded`: `grep -n "storeCredit.*creditGenerated\|creditGenerated.*storeCredit" src/lib/processReturn.ts` finds it.
  </acceptance_criteria>
  <done>processReturn applies haver capping for crediário (zero-paid → 0, partial → pro-rated), preserves cash-sale behavior, returns the open/overdue cancelled-installment set on full crediário returns; CreditPayment.type accepts 'abatimento'; and ReturnRecord carries the optional abatedInstallments map. The complete devolução type surface is defined in this plan (no Wave-2 plan edits src/types/index.ts). Build is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pure processAbatement function for the debt-abatement modality (DEV-05)</name>
  <files>src/lib/processReturn.ts</files>
  <read_first>
    - src/lib/processReturn.ts (after Task 1 — the module to extend)
    - src/lib/installmentStatus.ts (getEffectiveStatus — sort overdue first)
    - src/lib/formatters.ts (roundCurrency)
    - .planning/research/FINANCIAL-PITFALLS.md §3 (debt abatement algorithm, abatement loop, CreditPayment audit shape, A1–A4 edge cases)
    - src/types/index.ts (Installment, CreditPayment, ReturnRecord — abatedInstallments already added in Task 1)
  </read_first>
  <behavior>
    - Return R$50, one open installment with remaining R$50 → installment.discountApplied += 50, status → 'paid', creditPayments has one 'abatimento' entry of 50, residual = 0, creditGenerated = 0.
    - Return R$30, open installment remaining R$50 → discountApplied += 30, status stays 'open', residual = 0.
    - Return R$120 across two chosen installments R$50 + R$80 → first fully paid (50), second discountApplied += 70 (still open), residual = 0.
    - Return R$200 but chosen installments only absorb R$150 → residual = 50 returned explicitly; excess NEVER becomes haver and is NEVER silently applied to unselected installments.
    - Installments are consumed overdue-first, then by earliest dueDate.
    - discountApplied never decremented here; amountPaid untouched (abatement uses discountApplied per §3).
  </behavior>
  <action>
    Add and export `processAbatement(input: ProcessAbatementInput): ProcessAbatementResult` to src/lib/processReturn.ts. Define ProcessAbatementInput as `{ sale: Sale; returnValue: number; selectedInstallmentIds: string[]; installments: Installment[]; clientId: string; clientName: string }`. Define ProcessAbatementResult as `{ updatedInstallments: Installment[]; creditPayments: CreditPayment[]; abatedTotal: number; residual: number; abatedMap: { installmentId: string; amount: number }[] }` (abatedMap matches the shape of the ReturnRecord.abatedInstallments field defined in Task 1). Implement per FINANCIAL-PITFALLS §3: filter the selected installments to only those whose getEffectiveStatus is 'open' or 'overdue', sort overdue-first then by earliest dueDate; iterate accumulating `remainingReturnValue` starting at roundCurrency(returnValue); for each, remaining = roundCurrency(i.amount - i.amountPaid - (i.discountApplied || 0)), abatement = roundCurrency(Math.min(remaining, remainingReturnValue)); skip if abatement <= 0; set discountApplied += abatement, and if amountPaid + discountApplied >= amount mark status 'paid' and paidAt = now; push a CreditPayment `{ id: crypto.randomUUID(), saleId: i.saleId, installmentId: i.id, clientId, clientName, amount: abatement, paymentMethod: 'cash', type: 'abatimento', createdAt: now }`; decrement remainingReturnValue. Build abatedMap of { installmentId, amount } for each abatement applied (this is the reversal key consumed by DEV-07 in Wave 2 via ReturnRecord.abatedInstallments). Set residual = roundCurrency(remainingReturnValue) at the end. The function is pure: it returns updatedInstallments (a full mapped array) and never reads/writes storage. Reference D-05 (DEV-05) in a comment. The "excess never silently becomes haver" invariant is satisfied by returning residual to the caller and applying nothing to non-selected installments.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -n "export function processAbatement\|export const processAbatement" src/lib/processReturn.ts` finds the export.
    - `grep -n "type: 'abatimento'" src/lib/processReturn.ts` confirms the audit CreditPayment uses the new type.
    - `grep -n "residual" src/lib/processReturn.ts` confirms residual is computed and returned (excess not converted to haver).
    - `grep -n "abatedMap" src/lib/processReturn.ts` confirms the reversal map is returned (shape matches ReturnRecord.abatedInstallments).
    - `grep -n "getEffectiveStatus" src/lib/processReturn.ts` confirms overdue-first selection (Task 1 already imports it).
    - No storeCredit mutation anywhere in processAbatement: `grep -c "storeCredit" src/lib/processReturn.ts` count unchanged from Task 1 (abatement adds none).
  </acceptance_criteria>
  <done>processAbatement reduces operator-chosen open/overdue installments by the return value (overdue-first), records auditable 'abatimento' CreditPayments, marks fully-covered installments paid, returns an explicit residual (never haver) and an abatedMap (matching ReturnRecord.abatedInstallments) for reversal. Build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator return selection → financial mutation | Operator chooses items/installments; capping + abatement rules decide haver/installment effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | haver base (processReturn) | mitigate | creditGenerated capped by crediarioPaid sum × proportion; zero-paid → 0; ground-truth from installment.amountPaid not stale sale.crediarioPaid |
| T-04-02 | Repudiation | abatement audit | mitigate | each abatement writes a CreditPayment with type 'abatimento' + installmentId + amount + createdAt |
| T-04-03 | Elevation | excess abatement → haver | mitigate | residual returned explicitly to caller; processAbatement never touches storeCredit nor unselected installments |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | none — no new packages introduced in this phase |
</threat_model>

<verification>
- `npm run build` exits 0 after each task.
- processReturn caps haver for crediário (zero-paid → 0, partial → pro-rated, cash → unchanged) and returns the open/overdue cancelled set on full returns.
- processAbatement reduces chosen installments without generating haver, with explicit residual and audit CreditPayments.
- CreditPayment.type accepts 'abatimento' and ReturnRecord accepts abatedInstallments without breaking existing records (complete Wave-2 type surface defined here).
</verification>

<success_criteria>
- DEV-04: haver generated only up to the proportion actually paid for crediário sales; cash-sale haver preserved.
- DEV-05: pure abatement logic exists, generates no haver, records auditable abatimento, returns residual.
- The module remains pure and backward-compatible with existing Sales.tsx / Returns.tsx call sites (new fields are additive; installments param treated as optional).
- The full devolução type surface (CreditPayment 'abatimento' + ReturnRecord.abatedInstallments?) is owned by this plan; no Wave-2 plan edits src/types/index.ts.
</success_criteria>

<output>
Create `.planning/phases/04-devolucao-completa/04-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
