---
phase: 05-venda-fracionada
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pages/Products.tsx
autonomous: true
requirements: [FRAC-03]
must_haves:
  truths:
    - "Opening a product with stock 8.5 in the edit dialog and saving without changing stock preserves 8.5 (not truncated to 8)"
    - "The stock input accepts decimals for measure units (step derived from unit)"
    - "Entering a decimal stock for a discrete unit clamps to an integer on save"
  artifacts:
    - path: "src/pages/Products.tsx"
      provides: "Decimal-preserving stock field"
      contains: "parseFloat"
  key_links:
    - from: "src/pages/Products.tsx"
      to: "src/lib/units.ts"
      via: "import quantityStep, clampQuantityForUnit"
      pattern: "import .*units"
---

<objective>
Stop the product edit form from truncating fractional stock. After selling 1.5 mt from a 10 mt roll, stock is 8.5; today opening and saving that product in the dialog rounds it to 8 because the field uses `parseInt`. Switch to `parseFloat` and derive the input step from the unit.

Purpose: FRAC-03 (cadastro de produto preserva estoque decimal). Fixes the BUG-5-related `parseInt` at Products.tsx line ~324.
Output: Modified `src/pages/Products.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-venda-fracionada/05-CONTEXT.md
@.planning/phases/05-venda-fracionada/05-01-PLAN.md

<read_first>
- src/lib/units.ts — Plan 01 helper: `quantityStep`, `clampQuantityForUnit`, `isFractionalUnit`.
- src/pages/Products.tsx line 84 — `const units = ['un', 'kg', 'mt', 'cx', 'pc', 'lt', 'par', 'jg', 'rl', 'mil'];` (the unit dropdown source; do not change this list).
- src/pages/Products.tsx lines 240-251 — the Unidade `<Select>` bound to `formData.unit`; this is the source of the current unit when editing the stock field.
- src/pages/Products.tsx lines 318-326 — the Estoque `<Input type="number">` whose onChange uses `parseInt(e.target.value) || 0` at line 324. This is the only line that must change for the parse fix.
- src/pages/Products.tsx lines 300-315 — note the price/costPrice inputs already use `parseFloat(...) || 0` with `step="0.01"`; mirror that style for the stock field.
- src/pages/Products.tsx ~line 167 — `handleSave`/form submit path (where `formData` is written to a Product). Confirm where to apply the discrete-unit clamp on save.
</read_first>

<interfaces>
From src/lib/units.ts (Plan 01):
- `quantityStep(unit: string): number`  // 0.5 fractional, 1 discrete
- `clampQuantityForUnit(quantity: number, unit: string): number`  // rounds discrete units to integer
- `isFractionalUnit(unit: string): boolean`

`formData.unit` holds the currently-selected unit string in the edit dialog; use it to derive the stock input step and the save-time clamp.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the stock field decimal-preserving and unit-aware</name>
  <files>src/pages/Products.tsx</files>
  <behavior>
    - Editing a product whose stock is 8.5 and saving without touching the stock field keeps 8.5.
    - Typing 2.5 into the stock field for an mt product stores 2.5.
    - Typing 2.5 for an un product results in an integer stock on save (clamped).
    - Empty stock input falls back to 0 (preserve the `|| 0` behavior).
  </behavior>
  <action>
    Import the helper: `import { quantityStep, clampQuantityForUnit } from '@/lib/units';`.

    (a) Stock input (line ~320-325): change `parseInt(e.target.value) || 0` to `parseFloat(e.target.value) || 0` (FRAC-03 core fix). Add a `step` attribute derived from the selected unit: `step={quantityStep(formData.unit)}` and keep/add `min="0"`. This mirrors the price field's `parseFloat` + `step` pattern already in the file.

    (b) Save-time clamp: in the save path (`handleSave` / where `formData` becomes a Product, ~line 167), apply `clampQuantityForUnit(formData.stock, formData.unit)` to the stock value being persisted, so discrete units (un/cx/mil) cannot be saved with a fractional stock while fractional units keep decimals. Apply this clamp once at save, not on every keystroke (so the operator can type freely).

    Do not change the `units` array, the unit `<Select>`, or any other field. Do not touch `minStock` handling unless it also uses parseInt for a measure context — if `minStock` uses parseInt and the same decimal concern applies, you may apply the same parseFloat fix, but the required scope is the `stock` field only.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/pages/Products.tsx','utf8');if(!s.includes(\"from '@/lib/units'\"))throw new Error('units import missing');const stockBlock=s.substring(s.indexOf('id=\"stock\"'),s.indexOf('id=\"stock\"')+400);if(stockBlock.includes('parseInt'))throw new Error('stock field still uses parseInt');if(!stockBlock.includes('parseFloat'))throw new Error('stock field not parseFloat');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `import ... from '@/lib/units'` present in Products.tsx.
    - The stock `<Input>` onChange uses `parseFloat(...) || 0`, not `parseInt`; grep within the `id="stock"` block confirms no `parseInt`.
    - The stock input has a `step` derived from `quantityStep(formData.unit)`.
    - The save path applies `clampQuantityForUnit(formData.stock, formData.unit)` so discrete units persist integer stock.
  </acceptance_criteria>
  <done>Editing and saving a product preserves decimal stock for measure units and clamps discrete units to integers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build verification</name>
  <files>src/pages/Products.tsx</files>
  <read_first>
    - src/pages/Products.tsx (changes from Task 1)
  </read_first>
  <action>
    Build the project and fix any compile error introduced by the Products.tsx changes.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Products.tsx compiles clean with decimal stock support.</done>
</task>

</tasks>

<verification>
- Stock field uses parseFloat with a unit-derived step; no parseInt remains in the stock input.
- Save path clamps discrete units to integer stock.
- `npm run build` exits 0.
</verification>

<success_criteria>
Opening and saving a product preserves its decimal stock value for measure units (no truncation by parseInt), while discrete units remain integer (FRAC-03).
</success_criteria>

<output>
Create `.planning/phases/05-venda-fracionada/05-04-SUMMARY.md` when done.
</output>
