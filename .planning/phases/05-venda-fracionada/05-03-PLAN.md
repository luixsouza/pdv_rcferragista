---
phase: 05-venda-fracionada
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pages/Quotes.tsx
autonomous: true
requirements: [FRAC-02, FRAC-04]
must_haves:
  truths:
    - "Typing 1,5 in a quote line quantity for an mt product sets quantity 1.5 (not truncated)"
    - "The +/- buttons move an mt/kg/lt quote line by 0.5 and an un/cx line by 1"
    - "Quote line total = roundCurrency(1.5 * unitPrice) with no centavo float error"
    - "A discrete-unit quote line cannot be set to a fractional quantity"
  artifacts:
    - path: "src/pages/Quotes.tsx"
      provides: "Fractional quantity input + unit-aware step + rounded line totals in quotes"
      contains: "from '@/lib/units'"
  key_links:
    - from: "src/pages/Quotes.tsx"
      to: "src/lib/units.ts"
      via: "import isFractionalUnit, quantityStep, parseQuantity, clampQuantityForUnit"
      pattern: "import .*units"
    - from: "src/pages/Quotes.tsx"
      to: "src/lib/formatters.ts"
      via: "roundCurrency on line totals"
      pattern: "roundCurrency"
---

<objective>
Bring the orçamento (quote) builder to parity with the PDV for fractional quantities: decimal input for measure units, per-unit +/- step, integer-only discrete units, and rounded line totals.

Purpose: FRAC-04 (quote accepts fractional quantity for measure units) and FRAC-02 (line total computed with correct rounding). Same mechanical change as POS, applied to Quotes.tsx.
Output: Modified `src/pages/Quotes.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-venda-fracionada/05-CONTEXT.md
@.planning/phases/05-venda-fracionada/05-01-PLAN.md

<read_first>
- src/lib/units.ts — Plan 01 helper: `isFractionalUnit`, `quantityStep`, `parseQuantity`, `clampQuantityForUnit`.
- src/lib/formatters.ts — `roundCurrency`, `formatCurrency`. Check whether Quotes.tsx already imports `roundCurrency`; if not, add it.
- src/pages/Quotes.tsx lines 60-90 — add-to-cart logic (`addProduct`/equivalent): uses `isMilheiro`, `unitPrice = price/1000` for mil, initial `quantity: 1`, line `total = unitPrice` (raw, no roundCurrency currently).
- src/pages/Quotes.tsx lines 96-121 — `updateQuantity(productId, delta)` (the +/- buttons; `newQuantity = item.quantity + delta`; line `total = newQuantity * i.unitPrice`, raw float).
- src/pages/Quotes.tsx lines 123-144 — `updateItemQuantity(productId, newQuantity)` (direct set from input; line `total = newQuantity * i.unitPrice`, raw float).
- src/pages/Quotes.tsx lines 298-324 — the quote line `<Input type="number">` (onChange uses `parseInt` at line 312) and the Minus/Plus `<Button>`s calling `updateQuantity(item.productId, -1)` / `updateQuantity(item.productId, 1)`.
- NOTE: Quotes currently does NOT round line totals (lines 75, 88, 118, 141 use raw float). This plan adds roundCurrency on those line totals for centavo correctness (FRAC-02).
</read_first>

<interfaces>
From src/lib/units.ts (Plan 01):
- `quantityStep(unit: string): number`  // 0.5 fractional, 1 discrete
- `parseQuantity(raw: string): number`  // '1,5' -> 1.5, NaN if unparseable
- `clampQuantityForUnit(quantity: number, unit: string): number`

Quote cart item shape mirrors POS: `{ productId, productName, quantity, unitPrice, total, ... }`. Look up unit via `products.find(p => p.id === productId)?.unit`. `mil` stays a discrete selling unit (cart-level quantity whole; ÷1000 only on price).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire fractional quantity + rounded totals into Quotes cart</name>
  <files>src/pages/Quotes.tsx</files>
  <behavior>
    - Typing "1,5" in a quote line qty for an mt product sets quantity 1.5 and line total roundCurrency(1.5 * unitPrice).
    - Clicking + on an mt line increases by 0.5; on an un line increases by 1.
    - Setting an un line to 1.5 via the input clamps to an integer.
    - Empty/NaN input does not crash (existing isNaN guard preserved).
  </behavior>
  <action>
    Import helper: add `import { quantityStep, parseQuantity, clampQuantityForUnit } from '@/lib/units';`. Ensure `roundCurrency` is imported from `@/lib/formatters` (add it to the existing formatters import if absent).

    (a) Quote line quantity input (lines ~307-316): replace `parseInt(e.target.value)` with `parseQuantity(e.target.value)`; keep the `if (!isNaN(val)) updateItemQuantity(...)` guard. Add `step={quantityStep(products.find(p => p.id === item.productId)?.unit)}` and `min="0"` to the `<Input>` (FRAC-04 per-unit step).

    (b) +/- buttons (lines ~303 and ~321): compute the line's step (`const step = quantityStep(products.find(p => p.id === item.productId)?.unit)`) and pass `updateQuantity(item.productId, -step)` / `updateQuantity(item.productId, step)` instead of literal ±1.

    (c) `updateItemQuantity` (lines ~123-144): clamp via `clampQuantityForUnit(newQuantity, product.unit)` before applying; set the line total to `roundCurrency(newQuantity * i.unitPrice)` (FRAC-02 — replaces the current raw `newQuantity * i.unitPrice`). Preserve the existing stock-warning toast.

    (d) `updateQuantity` (lines ~96-121): delta is now fractional; round the resulting quantity to avoid float drift (`Math.round(newQuantity * 100) / 100`), and set line total to `roundCurrency(newQuantity * i.unitPrice)`. Keep the `<= 0` removal and stock-warning behavior.

    (e) Add-to-cart line (lines ~75, ~88): wrap the initial/incremented line totals in `roundCurrency(...)` for consistency. Do not change the initial quantity (1).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/pages/Quotes.tsx','utf8');if(!s.includes(\"from '@/lib/units'\"))throw new Error('units import missing');if(/value={item.quantity}[\s\S]{0,120}parseInt/.test(s))throw new Error('quote qty input still parseInt');if(!s.includes('quantityStep'))throw new Error('quantityStep not wired');if(!s.includes('roundCurrency'))throw new Error('roundCurrency not used');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `import ... from '@/lib/units'` present in Quotes.tsx; `roundCurrency` imported from formatters.
    - Quote quantity input onChange uses `parseQuantity`, not `parseInt` (grep confirms no parseInt in the quantity input block).
    - +/- buttons use a unit-derived step (`quantityStep` referenced).
    - `updateItemQuantity` and `updateQuantity` set line total via `roundCurrency` and clamp discrete units.
  </acceptance_criteria>
  <done>Quote lines accept 1,5 mt with rounded totals; discrete units stay integer; +/- step matches unit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build verification</name>
  <files>src/pages/Quotes.tsx</files>
  <read_first>
    - src/pages/Quotes.tsx (changes from Task 1)
  </read_first>
  <action>
    Build the project and fix any type/compile error introduced by the Quotes.tsx changes. No new behavior here.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Quotes.tsx compiles clean with fractional quantity support.</done>
</task>

</tasks>

<verification>
- Quotes imports and uses units.ts helpers; quantity input no longer truncates.
- Line totals use roundCurrency; discrete units clamped to integer.
- `npm run build` exits 0.
</verification>

<success_criteria>
The orçamento accepts and displays fractional quantities for measure units with correct rounded totals, matching the PDV behavior (FRAC-04, FRAC-02).
</success_criteria>

<output>
Create `.planning/phases/05-venda-fracionada/05-03-SUMMARY.md` when done.
</output>
