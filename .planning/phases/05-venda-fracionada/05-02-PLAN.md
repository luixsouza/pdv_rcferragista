---
phase: 05-venda-fracionada
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pages/POS.tsx
autonomous: true
requirements: [FRAC-01, FRAC-02, FRAC-03]
must_haves:
  truths:
    - "Typing 1,5 in the cart quantity field for an mt product sets quantity 1.5 (not truncated to 1)"
    - "The +/- buttons move an mt/kg/lt item by 0.5 and an un/cx item by 1"
    - "Item total displays roundCurrency(1.5 * unitPrice)"
    - "A discrete-unit (un) item cannot be set to 1.5 — it is clamped to an integer"
    - "Finalizing a sale of 2.5 mt deducts 2.5 from product stock (mil rule still divides by 1000)"
  artifacts:
    - path: "src/pages/POS.tsx"
      provides: "Fractional cart quantity input, unit-aware +/- step, decimal stock deduction"
      contains: "from '@/lib/units'"
  key_links:
    - from: "src/pages/POS.tsx"
      to: "src/lib/units.ts"
      via: "import isFractionalUnit, quantityStep, parseQuantity, clampQuantityForUnit"
      pattern: "import .*units"
    - from: "src/pages/POS.tsx updateQuantity"
      to: "quantityStep(product.unit)"
      via: "step replaces hardcoded ±1 delta"
      pattern: "quantityStep"
---

<objective>
Make the POS cart accept and compute decimal quantities for measure units (mt, kg, lt, m²) while keeping discrete units (un, cx, etc.) integer-only. Wire the cart quantity input, the +/- buttons, and the in-cart quantity setter to the shared `units.ts` helper, and confirm stock deduction respects decimals.

Purpose: FRAC-01 (decimal input + per-unit step), FRAC-02 (item/sale total via roundCurrency), FRAC-03 (decimal stock deduction in finalizeSale). Fixes BUG-5.
Output: Modified `src/pages/POS.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-venda-fracionada/05-CONTEXT.md
@.planning/phases/05-venda-fracionada/05-01-PLAN.md

<read_first>
- src/lib/units.ts — the helper created in Plan 01: `isFractionalUnit`, `quantityStep`, `parseQuantity`, `clampQuantityForUnit`, `FRACTIONAL_UNITS`.
- src/lib/formatters.ts — `roundCurrency`, `formatCurrency` (already imported in POS.tsx).
- src/pages/POS.tsx lines 359-394 — `addToCart` (adds quantity 1, `+1` for existing; uses `isMilheiro`/`effectiveStock`/`unitPrice`).
- src/pages/POS.tsx lines 396-422 — `updateQuantity(productId, delta)` (the +/- buttons; currently delta is the literal ±1 passed from JSX; computes `newQuantity = item.quantity + delta`).
- src/pages/POS.tsx lines 424-446 — `updateItemQuantity(productId, newQuantity)` (direct set from the input; clamps to effectiveStock).
- src/pages/POS.tsx lines 606-614 — stock deduction in finalizeSale: `deduction = product.unit === 'mil' ? cartItem.quantity / 1000 : cartItem.quantity` then `product.stock - deduction`. This already passes decimals through; verify it stays intact.
- src/pages/POS.tsx lines 1070-1094 — the cart quantity `<Input type="number">` (onChange uses `parseInt` at line 1084) and the Minus/Plus `<Button>`s calling `updateQuantity(item.productId, -1)` / `updateQuantity(item.productId, 1)`.
- IMPORTANT — do NOT touch the RETURN dialog quantity input at lines ~810-828 (`parseInt` at 817). That belongs to the devolução flow (Phase 4 scope), not fractional sale. Leave it as integer.
</read_first>

<interfaces>
From src/lib/units.ts (Plan 01):
- `isFractionalUnit(unit: string): boolean`
- `quantityStep(unit: string): number`  // 0.5 for mt/kg/lt/m², 1 otherwise
- `parseQuantity(raw: string): number`  // normalizes '1,5' -> 1.5, returns NaN if unparseable
- `clampQuantityForUnit(quantity: number, unit: string): number`  // rounds discrete units to integer

The cart item shape (`cart` state): `{ productId, productName, quantity, unitPrice, costPrice, total }`. `unitPrice` is already price-adjusted for `mil` (÷1000) at add time. To get the product's unit inside handlers, look it up: `products.find(p => p.id === productId)?.unit`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire fractional input + per-unit step into cart quantity handlers</name>
  <files>src/pages/POS.tsx</files>
  <behavior>
    - Typing "1,5" (or "1.5") in the cart qty input for an mt product results in cart quantity 1.5 and total roundCurrency(1.5 * unitPrice).
    - Clicking + on an mt item increases quantity by 0.5; clicking + on an un item increases by 1.
    - Setting an un item's quantity to 1.5 via the input clamps it to an integer (2 after rounding).
    - Empty/NaN input does not crash and does not set quantity to NaN (existing isNaN guard preserved).
  </behavior>
  <action>
    Import the helper at the top of POS.tsx: add `import { isFractionalUnit, quantityStep, parseQuantity, clampQuantityForUnit } from '@/lib/units';` alongside the existing `@/lib/formatters` import.

    (a) Cart quantity input (lines ~1079-1088): replace `parseInt(e.target.value)` with `parseQuantity(e.target.value)`. Keep the `if (!isNaN(val)) updateItemQuantity(...)` guard. Add a `step` attribute derived from the item's unit: look up the product unit (`products.find(p => p.id === item.productId)?.unit`) and set `step={quantityStep(unit)}` and `min="0"`. This satisfies FRAC-01's per-unit step on the input.

    (b) +/- buttons (lines ~1075 and ~1093): change the literal `-1` / `1` deltas so the step matches the unit. Compute the unit's step once per cart row (e.g. `const step = quantityStep(products.find(p => p.id === item.productId)?.unit)`) and pass `updateQuantity(item.productId, -step)` and `updateQuantity(item.productId, step)`.

    (c) `updateItemQuantity` (lines ~424-446): before applying, clamp via `clampQuantityForUnit(newQuantity, product.unit)` so discrete units cannot become fractional (enforces "não permitir 1,5 un"). Keep the existing effectiveStock check and the `roundCurrency(newQuantity * i.unitPrice)` total computation (FRAC-02 — already present, do not remove).

    (d) `updateQuantity` (lines ~396-422): it already computes `newQuantity = item.quantity + delta`; now delta is fractional. Guard against floating-point drift by applying `roundCurrency`-style cleanup to the quantity itself is NOT needed for money, but round the quantity to a sane precision (e.g. `Math.round(newQuantity * 100) / 100`) to avoid 0.30000000000000004 from repeated 0.1 additions. Keep the existing effectiveStock and `<= 0` removal logic and the `roundCurrency(newQuantity * i.unitPrice)` total.

    Do NOT modify `addToCart` (initial quantity 1 is correct for both fractional and discrete — the operator refines via input/buttons). Do NOT touch the return dialog input at lines ~810-828.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/pages/POS.tsx','utf8');const seg=s.split(/updateQuantity = /)[0];const cart=s.substring(s.indexOf('value={item.quantity}'));if(/value={item.quantity}[\s\S]{0,120}parseInt/.test(s))throw new Error('cart input still uses parseInt');if(!s.includes(\"from '@/lib/units'\"))throw new Error('units import missing');if(!s.includes('quantityStep'))throw new Error('quantityStep not wired');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `import ... from '@/lib/units'` present in POS.tsx.
    - The cart quantity input onChange no longer uses `parseInt` (uses `parseQuantity`); grep confirms no `parseInt` within the `value={item.quantity}` input block.
    - The +/- buttons pass a unit-derived step (reference to `quantityStep` present near `updateQuantity` calls).
    - `updateItemQuantity` calls `clampQuantityForUnit`.
    - The return-dialog input (lines ~810-828) is unchanged (still `parseInt`).
  </acceptance_criteria>
  <done>Cart accepts 1,5 mt with total = roundCurrency(1.5 * unitPrice); un items stay integer; +/- step matches unit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Confirm decimal stock deduction and build</name>
  <files>src/pages/POS.tsx</files>
  <read_first>
    - src/pages/POS.tsx lines 606-614 (finalizeSale stock deduction)
  </read_first>
  <action>
    Inspect the finalizeSale stock-update block (lines ~606-614). Confirm it deducts `cartItem.quantity` directly (with the `mil` ÷1000 branch) so a 2.5 mt sale subtracts 2.5 from `product.stock` — no `parseInt`/`Math.floor`/`Math.round` truncation anywhere in the deduction path. If any integer truncation exists in the deduction, remove it so decimals pass through (FRAC-03). The existing code at line 610-611 already does `product.unit === 'mil' ? cartItem.quantity / 1000 : cartItem.quantity` — leave the mil rule intact; only ensure no rounding wraps it. Then build.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - Stock deduction in finalizeSale uses `cartItem.quantity` (mil branch ÷1000) with no integer truncation; grep shows no `parseInt`/`Math.floor` in the deduction block.
    - A 2.5 mt sale would subtract 2.5 from stock (verified by source inspection of the deduction expression).
  </acceptance_criteria>
  <done>finalizeSale deducts decimal quantities correctly while preserving the mil milheiro rule; POS builds clean.</done>
</task>

</tasks>

<verification>
- units.ts helpers imported and wired into the cart input, +/- buttons, and updateItemQuantity.
- Cart quantity input no longer truncates via parseInt; discrete units clamped to integers.
- Stock deduction passes decimals through with mil rule intact.
- `npm run build` exits 0.
</verification>

<success_criteria>
In the PDV, adding an mt/kg/lt product lets the operator enter decimal quantity with a 0.5 +/- step, the item and sale totals compute via roundCurrency, discrete units stay integer, and finalizing deducts the decimal quantity from stock.
</success_criteria>

<output>
Create `.planning/phases/05-venda-fracionada/05-02-SUMMARY.md` when done.
</output>
