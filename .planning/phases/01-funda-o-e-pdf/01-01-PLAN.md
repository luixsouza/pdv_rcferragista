---
phase: 01-funda-o-e-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/formatters.ts
  - src/types/index.ts
  - src/pages/POS.tsx
autonomous: true
requirements: [FND-01, FND-03]
must_haves:
  truths:
    - "roundCurrency(v) is exported from src/lib/formatters.ts and returns Math.round(v*100)/100"
    - "Generated crediário installments sum exactly to crediarioFinanced (no R$0,01 floating-point gap)"
    - "Cart item totals are rounded to centavos at the calculation boundary"
    - "ReturnRecord type declares optional cancelledInstallmentIds without breaking stored data"
    - "Existing visible behavior is unchanged except centavo correctness"
  artifacts:
    - path: "src/lib/formatters.ts"
      provides: "roundCurrency helper"
      contains: "export const roundCurrency"
    - path: "src/types/index.ts"
      provides: "ReturnRecord.cancelledInstallmentIds optional field"
      contains: "cancelledInstallmentIds"
    - path: "src/pages/POS.tsx"
      provides: "Rounded installment generation with last-installment residual correction"
  key_links:
    - from: "src/pages/POS.tsx"
      to: "src/lib/formatters.ts"
      via: "import roundCurrency"
      pattern: "roundCurrency"
---

<objective>
Add the shared `roundCurrency` money-rounding helper (FND-01) and the backward-compatible type changes (FND-03) that unblock every later phase. Apply `roundCurrency` at the existing money-calculation boundaries in POS.tsx — installment generation (with last-installment residual absorption) and cart item totals — WITHOUT changing visible behavior beyond centavo correctness.

Purpose: Establish a single rounding primitive and the type surface (cancelled installment tracking, decimal quantity) that Phases 3, 4 and 5 depend on, while eliminating R$0,01 floating-point drift in installment math today.
Output: `roundCurrency` exported from formatters.ts; `ReturnRecord.cancelledInstallmentIds?` field; rounded cart/installment math in POS.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/FINANCIAL-PITFALLS.md

<interfaces>
<!-- Existing contracts the executor needs. No codebase exploration required. -->

From src/lib/formatters.ts (current — add roundCurrency alongside):
```typescript
export const formatCurrency = (value: number) => { /* Intl pt-BR BRL */ };
export const paymentLabels: Record<string, string> = { /* ... */ };
```

From src/lib/cardFees.ts:62 — existing rounding pattern to mirror:
`Math.round(x * 100) / 100`

From src/types/index.ts — current ReturnRecord (line 100) and SaleItem (line 31):
```typescript
export interface SaleItem {
  productId: string;
  productName: string;
  quantity: number;   // already number — supports decimal; NO migration needed
  unitPrice: number;
  costPrice?: number;
  total: number;
}
export interface ReturnRecord {
  id: string;
  originalSaleId: string;
  clientId: string;
  clientName: string;
  items: SaleItem[];
  totalRefunded: number;
  creditGenerated: number;
  createdAt: string;
  reversedAt?: string;
}
```

From src/pages/POS.tsx — money boundaries to round:
- Line 118: `const subtotal = cart.reduce((sum, item) => sum + item.total, 0);`
- Line 147: `const installmentValue = installmentCount > 0 ? crediarioFinanced / installmentCount : 0;`
- Lines 172, 186, 217, 241: cart item `total` computed as `quantity * unitPrice`
- Lines 442-457: installment loop assigns `amount: installmentValue` to each of N installments (number 1..N)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add roundCurrency helper and ReturnRecord field</name>
  <read_first>
    - src/lib/formatters.ts (full file — append the helper)
    - src/lib/cardFees.ts (line ~62 — confirm the existing Math.round(x*100)/100 pattern to mirror)
    - src/types/index.ts (full file — locate ReturnRecord at line 100 and SaleItem at line 31)
    - .planning/research/FINANCIAL-PITFALLS.md (section 4 — rounding strategy and the JSDoc text)
  </read_first>
  <action>
    In src/lib/formatters.ts add an exported `roundCurrency(value: number): number` that returns `Math.round(value * 100) / 100`, with a short JSDoc note that it rounds to centavos (round-half-up) to avoid the 0.1+0.2 float trap, mirroring the pattern in cardFees.ts:62. Do NOT add any dependency (no Decimal.js).
    In src/types/index.ts add an optional field `cancelledInstallmentIds?: string[]` to the `ReturnRecord` interface (after `reversedAt?`). Keep it optional so existing electron-store records without the field still deserialize (FND-03 retrocompatibility). Confirm `SaleItem.quantity` is already typed `number` — do NOT add or change any quantity type; no data migration is part of this phase (decimal UI is Phase 5).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/formatters.ts` contains `export const roundCurrency` returning `Math.round(value * 100) / 100`
    - `src/types/index.ts` ReturnRecord contains `cancelledInstallmentIds?: string[]`
    - `SaleItem.quantity` remains typed `number` (no change)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>roundCurrency exported; ReturnRecord has optional cancelledInstallmentIds; build passes; no new dependency added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply roundCurrency to POS cart totals and installment generation</name>
  <read_first>
    - src/pages/POS.tsx (lines 118-147 cart/crediário math; lines 157-244 addToCart/updateQuantity/updateItemQuantity; lines 404-460 finalizeSale stock + installment generation)
    - src/lib/formatters.ts (after Task 1 — import roundCurrency from here using the @/ alias)
    - .planning/research/FINANCIAL-PITFALLS.md (section 4 "Last-Installment Correction" and "Application Sites" table)
  </read_first>
  <action>
    Import `roundCurrency` from `@/lib/formatters` in POS.tsx.
    Wrap each cart item `total` computation `quantity * unitPrice` in `roundCurrency(...)` at all four sites: addToCart (lines ~172 and ~186), updateQuantity (~217), updateItemQuantity (~241).
    Replace the installment generation so installments sum EXACTLY to `crediarioFinanced`: compute `baseInstallment = roundCurrency(crediarioFinanced / installmentCount)`; assign `baseInstallment` to installments number 1..(N-1); assign the last installment (number N) `roundCurrency(crediarioFinanced - baseInstallment * (installmentCount - 1))` so the residual is absorbed. Keep the entry installment (number 0) amount as `entryAmount` rounded with `roundCurrency(entryAmount)`. Do NOT change due-date logic, status logic, or any other field. Do NOT change `installmentValue` used elsewhere for display unless needed for parity — keep visible totals identical aside from centavo correctness.
    No fenced code: implement directly in the existing loop at lines 442-457.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - POS.tsx imports `roundCurrency` from `@/lib/formatters`
    - All four cart `total` sites wrap the product in `roundCurrency`
    - Last installment uses `roundCurrency(crediarioFinanced - baseInstallment * (installmentCount - 1))`
    - Observable: a R$100 / 3-installment crediário sale produces installments 33.33 + 33.33 + 33.34 = 100.00 exactly (manual verification scenario F1)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Cart totals and generated installments are centavo-correct; installment sum equals crediarioFinanced exactly; build passes; no other behavior changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input → cart/installment math | Quantities and amounts entered by operator flow into money calculations persisted to electron-store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | installment amount math (POS.tsx) | mitigate | Last-installment residual absorption guarantees sum integrity; no rounding drift can silently alter financed total |
| T-01-02 | Integrity | type change to ReturnRecord | accept | Field is optional and additive; stored electron-store records remain valid (no PII, local single-user app) |
</threat_model>

<verification>
- `npm run build` exits 0 with all changes.
- Manual scenario F1: R$100 financed over 3 installments → 33.33 + 33.33 + 33.34 = 100.00.
- Manual scenario F2: 2.5 × R$12.99 cart item → total R$32.48 (rounded), not 32.475.
- Loading an existing electron-store data folder still parses (cancelledInstallmentIds absent on old records is fine).
</verification>

<success_criteria>
- `roundCurrency` exists and is the single rounding primitive used at the POS money boundaries.
- Installments sum exactly to the financed total.
- `ReturnRecord.cancelledInstallmentIds?` exists, backward compatible.
- No visible behavior change beyond centavo correctness.
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-e-pdf/01-01-SUMMARY.md` when done.
</output>
