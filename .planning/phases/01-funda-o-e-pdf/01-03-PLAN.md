---
phase: 01-funda-o-e-pdf
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/processReturn.ts
  - src/pages/Sales.tsx
  - src/pages/Returns.tsx
autonomous: true
requirements: [FND-02]
must_haves:
  truths:
    - "src/lib/processReturn.ts exists as a pure function (no storage side effects, no React)"
    - "processReturn returns the mutations to apply: the new ReturnRecord, product stock updates, updated client store credit, and a fully-returned flag"
    - "Sales.tsx handleReturnFromSale consumes processReturn"
    - "Returns.tsx handleReturn consumes processReturn"
    - "Devolução behavior is identical to today (behavioral parity): same haver amount, same stock restock, same refunded-status flip; NO installment cancellation and NO haver-capping introduced in this phase"
  artifacts:
    - path: "src/lib/processReturn.ts"
      provides: "Pure return-processing function consolidating duplicated logic"
      contains: "export function processReturn"
    - path: "src/pages/Sales.tsx"
      provides: "handleReturnFromSale refactored onto processReturn"
    - path: "src/pages/Returns.tsx"
      provides: "handleReturn refactored onto processReturn"
  key_links:
    - from: "src/pages/Sales.tsx"
      to: "src/lib/processReturn.ts"
      via: "import processReturn"
      pattern: "processReturn"
    - from: "src/pages/Returns.tsx"
      to: "src/lib/processReturn.ts"
      via: "import processReturn"
      pattern: "processReturn"
---

<objective>
Consolidate the duplicated devolução logic — `handleReturnFromSale` in Sales.tsx (~lines 233-306) and `handleReturn` in Returns.tsx (~lines 122-210) — into a single PURE function `src/lib/processReturn.ts` (FND-02). The function takes the sale, the items being returned, the existing products/clients, and whether a client is associated, and RETURNS the mutations to apply (new ReturnRecord, updated products array, updated clients array, allItemsReturned flag) with NO storage side effects. Both pages are refactored to call it and then apply the returned mutations via their existing `setX` setters.

CRITICAL — behavioral parity: this phase ONLY extracts and unifies the CURRENT behavior. Do NOT change haver rules (still `hasClient ? totalRefund : 0`), do NOT add haver-capping, and do NOT cancel installments. Those are Phase 3/4 and will be built on top of this base. The two existing call sites differ slightly (Returns.tsx validates max-returnable quantities before saving; Sales.tsx does not) — preserve each page's pre-call validation in the page; the extracted function covers the shared mutation logic only.

Purpose: Eliminate logic drift between the two return flows so the Phase 3/4 financial fixes are made in exactly one place.
Output: `src/lib/processReturn.ts` pure function; both pages refactored to consume it with identical observable behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/FINANCIAL-PITFALLS.md
@.planning/phases/01-funda-o-e-pdf/01-01-SUMMARY.md

<interfaces>
<!-- Current duplicated logic the function must reproduce with parity. -->

From src/types/index.ts (after Plan 01-01): ReturnRecord now has optional `cancelledInstallmentIds?: string[]` (leave unset this phase), SaleItem.quantity is `number`.

Shared mutation logic present in BOTH handlers today:
- Build ReturnRecord: id=crypto.randomUUID(), originalSaleId=sale.id, clientId = clientId || 'sem-cliente', clientName = client?.name || sale.clientName || 'Sem cliente', items mapped {productId, productName, quantity, unitPrice, costPrice, total: quantity*unitPrice}, totalRefunded = sum(quantity*unitPrice), creditGenerated = hasClient ? totalRefunded : 0, createdAt = now.
- Restore stock: for each returned item, `restock = product.unit === 'mil' ? quantity/1000 : quantity`; `stock + restock`, updatedAt set.
- Store credit: if hasClient, target client `storeCredit = (storeCredit||0) + totalRefunded`, updatedAt set.
- allItemsReturned: merge getReturnedQuantities(sale.id) with the items being returned; sale is fully returned when every sale item's cumulative returned qty >= its quantity. Caller flips sale.status to 'refunded' when true.

getReturnedQuantities(saleId) helper exists in Sales.tsx:200-209 and equivalently in Returns.tsx — it sums prior non-reversed returns per productId. The pure function should accept already-returned quantities (or the returns list) as an input rather than reading storage.

Caller setters that apply the returned mutations:
- Sales.tsx: setProducts, setClients, setSales, setReturns
- Returns.tsx: setProducts, setClients, setSales, setReturns
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create pure processReturn module</name>
  <read_first>
    - src/pages/Sales.tsx (lines 200-306 — getReturnedQuantities, returnTotal, handleReturnFromSale)
    - src/pages/Returns.tsx (lines 95-211 — returnedQtys, totalRefund, handleReturn; note the extra quantity validation at 132-141)
    - src/types/index.ts (ReturnRecord, SaleItem, Product, Client, Sale)
    - .planning/research/FINANCIAL-PITFALLS.md (section 6 — warning to run edge cases I1-I6 and the parity note: do NOT change haver/installments this phase)
  </read_first>
  <action>
    Create `src/lib/processReturn.ts` exporting a pure function `processReturn` (no React, no useLocalStorage, no storage writes). Define an input shape covering: the original `sale: Sale`, the selected returned items (productId, productName, quantity, unitPrice, costPrice), `products: Product[]`, `clients: Client[]`, the resolved `clientId` (or undefined for sem-cliente), and the map/list of already-returned quantities for that sale. Define a return shape covering: `returnRecord: ReturnRecord`, `updatedProducts: Product[]`, `updatedClients: Client[]`, and `allItemsReturned: boolean`.
    Implement exactly the shared logic described in <interfaces> with parity: totalRefunded = sum of quantity*unitPrice (apply `roundCurrency` per term to be consistent with Plan 01-01, since this only corrects centavos and does not change visible behavior), creditGenerated = hasClient ? totalRefunded : 0, stock restock with the `mil` /1000 rule, store-credit increment for the associated client, and the allItemsReturned computation. Do NOT set cancelledInstallmentIds, do NOT cancel installments, do NOT cap haver — leave those for Phase 3/4. Use `crypto.randomUUID()` and `new Date().toISOString()` exactly as the current code does.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/processReturn.ts` contains `export function processReturn` (pure — no imports from hooks/react, no window.electron, no setX)
    - Return shape includes returnRecord, updatedProducts, updatedClients, allItemsReturned
    - creditGenerated logic is unchanged (`hasClient ? totalRefunded : 0`) — no capping
    - No installment cancellation logic present
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Pure processReturn module exists, reproduces current shared return logic, sets no cancelledInstallmentIds, build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor Sales.tsx and Returns.tsx to consume processReturn</name>
  <read_first>
    - src/lib/processReturn.ts (after Task 1 — the input/return contract)
    - src/pages/Sales.tsx (lines 200-306 — handleReturnFromSale and getReturnedQuantities)
    - src/pages/Returns.tsx (lines 95-211 — handleReturn, including the validation block at 132-141)
  </read_first>
  <action>
    In Sales.tsx, refactor `handleReturnFromSale` to: keep its existing guards (selectedSale present, at least one selected item), build the returned-items list and the already-returned quantities as today, call `processReturn(...)`, then apply the result with the existing setters — setProducts(updatedProducts), setClients(updatedClients), setReturns([...returns, returnRecord]), and setSales(... status 'refunded' when allItemsReturned else unchanged). Keep the exact toast messages currently shown. Keep `getReturnedQuantities` as the source of already-returned quantities passed into processReturn.
    In Returns.tsx, refactor `handleReturn` the same way: KEEP its existing max-returnable quantity validation (lines ~132-141) before calling processReturn, then call processReturn and apply results via setProducts/setClients/setSales/setReturns and reset state as it does now. Keep its existing toast messages.
    Do not change `handleReverseReturn` in this phase. Do not change any haver amount or add installment handling. The observable result of both flows must be identical to before (behavioral parity); only the implementation is unified.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Sales.tsx imports and calls `processReturn`; handleReturnFromSale no longer inlines the ReturnRecord/stock/credit construction
    - Returns.tsx imports and calls `processReturn`; its pre-call quantity validation is preserved
    - Both pages still apply mutations through their existing setX setters
    - Toast messages unchanged in both pages
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Both return flows consume the shared pure function; behavior is identical to before; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator return selection → financial mutations | Returned items drive stock restock and store-credit (haver) which persist to electron-store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-RET-01 | Tampering | divergent return logic across two pages | mitigate | Single pure processReturn module removes drift; both call sites share one code path |
| T-01-RET-02 | Repudiation | behavioral regression during extraction | mitigate | Behavioral parity mandated; validate edge cases I1-I6 manually; build gate |
| T-01-RET-03 | Integrity | partial electron-store writes (non-transactional) | accept | Pre-existing constraint (OPS2-02, deferred v2); mutation order preserved as today |
</threat_model>

<verification>
- `npm run build` exits 0.
- Manual parity checks against current behavior:
  - I1-style: full devolução of a sale still flips status to refunded, restores stock, and (with client) adds totalRefunded as haver — same as before. (Installment cancellation is intentionally NOT added this phase.)
  - I2-style: partial devolução leaves sale status unchanged and restocks only the returned quantity.
  - Returns.tsx still blocks over-quantity returns with the same toast.error.
  - Sem-cliente return: no store credit generated, stock restored — same as before.
</verification>

<success_criteria>
- `src/lib/processReturn.ts` is a pure function with no storage side effects.
- Both Sales.tsx and Returns.tsx consume it with identical observable behavior.
- No haver-capping or installment cancellation introduced (deferred to Phase 3/4).
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-e-pdf/01-03-SUMMARY.md` when done.
</output>
